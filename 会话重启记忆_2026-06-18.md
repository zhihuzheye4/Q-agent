# 会话重启记忆 — 2026年06月18日

> **重要提示**：本次记录是电脑重启后 Claude Code 恢复上下文所需的唯一来源。请务必在每次启动时优先读取本文件。

---

## 1. 项目基础信息

| 项目 | 内容 |
|------|------|
| 项目名称 | Q-agent（原暂用名 gagent） |
| 本地路径 | `G:\agent` |
| GitHub 仓库 | https://github.com/zhihuzheye4/Q-agent |
| 仓库类型 | 公开 |
| GitHub 账号 | zhihuzheye4 |
| 当前阶段 | **从零开始，尚未实现任何功能代码** |

---

## 2. 本次会话完整时间线

### 2026-06-18 19:00 之前
- `G:\agent` 目录曾因 **Function 005 shell 工具测试事故** 被整体删除。
- 根因：`executor.run_shell('rm -rf /')` 被四级黑名单降级为 `clear_directory(path='.')`，而当时工作目录恰为 `G:/agent`，导致整个项目被清空。
- 已记录到全局错误追踪表：`F:\EnglishWenJian\claude-CeShi\工作错误追踪档案\错误追踪总表.md`，编号 **错误 053**。

### 2026-06-18 19:00 之后
- 用户回到家，输入"开始吧"，继续工作。
- 用户发现恢复后的文件混乱不可用，要求：
  1. 把本次错误记录到全局错误追踪总表。
  2. 删除 `G:\agent` 下所有内容，项目重新开始。
  3. 新建 Git 和 GitHub 仓库，命名为 **Q-agent**，公开。
  4. 每天 0:00 或记录记忆后自动同步到 GitHub。

### 已执行操作
- ✅ 更新全局错误追踪总表（错误 053）。
- ✅ 清空 `G:\agent` 目录（仅保留随后新建的 CLAUDE.md）。
- ✅ 重写 `G:\agent\CLAUDE.md`，标记当前阶段为"从零开始"。
- ✅ 初始化 Git 仓库。
- ✅ 创建 GitHub 公开仓库 `Q-agent`：https://github.com/zhihuzheye4/Q-agent
- ✅ 完成首次提交：`初始化项目：添加 CLAUDE.md 和 .gitignore`
- ✅ 创建自动同步脚本：`F:\EnglishWenJian\claude-CeShi\claude-data\scripts\q-agent-sync.py`
- ✅ 设置每天 00:01 自动同步的 Cron 任务（已持久化）。
- ✅ 创建本次重启记忆文件（本文件）。

---

## 3. 项目目标与架构方向

Q-agent 是一个**多技能编排型 Agent 框架**，参考 OpenClaw / Hermes / Codex / Claude 之长。

五层架构：
1. 对话/交互层
2. 编排/规划层
3. 记忆层
4. 技能/工具层
5. 执行/沙箱层

开发语言：Python。

架构原则（已写入 CLAUDE.md）：
- 不用网关
- 不过度抽象
- 不搞插件市场
- **项目根目录保护**：任何 shell/文件操作不得直接以 `G:/agent` 为工作目录或删除目标；写操作必须先 dry-run 并报告影响范围。

---

## 4. 下一步清单

重启后应优先完成：

1. **读取 `G:\agent\CLAUDE.md`**（全局规则要求启动时必读）。
2. **读取本文件**（`G:\agent\会话重启记忆_2026-06-18.md`）。
3. 重新搭建最小项目骨架：
   - `pyproject.toml`
   - `qagent/` 包目录
   - `qagent/__init__.py`
   - `qagent/__main__.py` 入口
4. 实现无风险的 `@skill` 注册体系。
5. 重新实现 shell 技能（必须锁定工作目录、禁止清理当前目录、所有写操作先 dry-run）。
6. 重新实现本地 LLM 聊天客户端基础版。
7. 设计并落地安全沙箱规则，防止再次误删项目根目录。

---

## 5. 关键文件与路径

| 用途 | 路径 |
|------|------|
| 项目入口 / 行为规则 | `G:\agent\CLAUDE.md` |
| 本次重启记忆 | `G:\agent\会话重启记忆_2026-06-18.md` |
| GitHub 仓库 | https://github.com/zhihuzheye4/Q-agent |
| 自动同步脚本 | `F:\EnglishWenJian\claude-CeShi\claude-data\scripts\q-agent-sync.py` |
| 全局错误追踪表 | `F:\EnglishWenJian\claude-CeShi\工作错误追踪档案\错误追踪总表.md` |
| 用户全局 CLAUDE.md | `C:\Users\sutai\.claude\CLAUDE.md` |

---

## 6. 重要决策记录

| 决策 | 结论 |
|------|------|
| 项目正式名 | Q-agent |
| 仓库可见性 | 公开 |
| 同步策略 | 每天 0:00 + 记录记忆后自动 commit + push |
| 是否保留旧 gagent 代码 | 否，已清空，从零开始 |
| shell 安全策略 | 必须锁定工作目录、禁止 `path='.'`、写操作先 dry-run |

---

## 7. 风险提醒

- 旧项目的 Function 005 shell 工具曾导致 `G:\agent` 整体被删，重启后重新实现 shell 功能时必须严格遵守 CLAUDE.md 中的"项目根目录保护"规则。
- GitHub Token 已用于配置本地推送，token 字符串不会保存到仓库文件中。
- 自动同步脚本位于 F 盘，不受 `G:\agent` 删除影响。

---

## 8. 用户最新指令

> "在根目录记录本次所有对话记录，电脑即将重启，这个是你重启后唯一记忆信息，所以务必详细"

本文件即为此指令的执行结果。重启后请优先读取本文件和 `G:\agent\CLAUDE.md`，然后按"下一步清单"继续。
