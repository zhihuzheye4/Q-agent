<!--
权限：L1 可写不可读
说明：本方案由 AI 在与用户讨论后生成。AI 执行时不回读本文件，避免被旧方案锚定。
     人类可查阅作为审计/历史。执行完毕后归档到 memory/方案/已执行/。
-->

# 方案：矢量图生成脚本开发（PySide6 + QtSvg + SVG sprite）

- 生成时间：2026-06-19 19:57
- 触发需求：UI 界面开发阶段，先生成矢量图标脚本，再实施 UI 框架
- 讨论结论：开发 `scripts/generate_icons.py`，先测脚本生成上限，再优化
- 状态：已完成

## 一、要做什么

按用户确定的工作流，开发矢量图生成脚本：
1. 写硬规则：CLAUDE.md 新增第二十节"分发零外部依赖"
2. 开发 `scripts/generate_icons.py`（5 级递进复杂度图标 + primitive + 渲染器 + 输出器 + manifest）
3. 跑脚本生成 5 个测试 SVG，验证上限
4. 优化线条细节，参考 Lucide / Feather 等主流图标库
5. 删除测试产物，清理 icons 目录
6. 记忆同步 + commit

## 二、涉及的文件

### 新增
- `G:\agent\scripts\generate_icons.py`（主脚本）
- `G:\agent\memory\方案\已执行\已执行_方案_2026-06-19_1957_矢量图生成脚本.md`（本方案归档）

### 修改
- `G:\agent\CLAUDE.md`（新增第二十节"分发零外部依赖硬规则"）
- `G:\agent\memory\ADR架构决策记录.md`（追加 ADR-013 PySide6 / ADR-014 矢量图脚本预制策略 / ADR-015 分发零外部依赖）
- `G:\agent\memory\思维导图数据.jsonl`（追加 2 行：分发零外部依赖硬规则 + 矢量图生成脚本）
- `G:\agent\memory\思维导图.md`（脚本重新渲染）
- `G:\agent\memory\技能工具注册清单.md`（注册 generate_icons 脚本）
- `G:\agent\q_agent\assets\README.md`（说明脚本用法 + 调用方案 + 硬规则）
- `G:\agent\memory\项目指导文件.md`（更新当前节点 + 已完成节点）
- `G:\agent\memory\临时记忆.md`（更新当前进度 + 下一步）

### 临时测试产物（生成后删除）
- `q_agent/assets/icons/circle-active.svg`（L1）
- `q_agent/assets/icons/send-active.svg`（L2）
- `q_agent/assets/icons/settings-active.svg`（L3）
- `q_agent/assets/icons/cloud-sync-active.svg`（L4）
- `q_agent/assets/icons/ai-brain-active.svg`（L5）
- `q_agent/assets/icons/manifest.json`

## 三、实施步骤

1. CLAUDE.md 追加第二十节"分发零外部依赖硬规则"
2. ADR 追加 ADR-013 / ADR-014 / ADR-015
3. 思维导图数据 jsonl 追加 2 行 → update_mindmap.py 渲染
4. 写 scripts/generate_icons.py（含 5 级递进复杂度图标 + primitive + 渲染器 + 输出器 + 清单 + clean）
5. ruff + mypy + format 全绿
6. 跑脚本生成 5 个测试 SVG → 验证 XML 结构
7. 修复 bug：SVG 标准属性名（stop-color/flood-color/flood-opacity/stroke-width）误用下划线
8. 优化线条细节：参考 Lucide / Feather 风格
   - 顶层加 stroke-linecap="round" + stroke-linejoin="round"
   - L1 circle: r=9 视觉饱满
   - L2 send: Lucide send 官方 path
   - L3 settings: Lucide settings 官方 path（单一复杂 path 描 8 齿轮廓 + 中心圆）
   - L4 cloud-sync: Lucide cloud 官方 path + 渐变 + refresh 风格同步箭头
   - L5 ai-brain: 大脑 path 优化（对称半球 + 沟回）+ 保留多色渐变/滤镜/电路
9. 重新生成验证
10. `--clean` 删除测试产物（icons/ 只留 .gitkeep）
11. assets/README.md 更新说明脚本用法
12. 技能工具注册清单注册 generate_icons
13. 项目指导文件更新当前节点
14. 临时记忆更新（含编年史归档）
15. 工具链全绿 + commit

## 四、验证方法

- `python scripts/generate_icons.py` 生成 5 个 SVG，文件大小 0.17~2.06 KB，生成时间 < 1ms
- `xml.etree.ElementTree.parse` 5 个 SVG 全部 well-formed
- L5 含 linearGradient / radialGradient / filter drop_shadow / 多 stop-color，全部正确渲染
- 修复后所有 SVG 属性用短横（stop-color 等），无下划线
- `ruff check` / `ruff format --check` / `mypy strict` 全绿
- `python scripts/generate_icons.py --clean` 删除 6 个测试产物（5 SVG + manifest.json），icons/ 只剩 .gitkeep
- CLAUDE.md 第二十节存在
- ADR-013/014/015 已追加
- 思维导图 md 含新增 2 个节点

## 五、风险与边界

- **PySide6 未装**：脚本默认 SVG-only 模式，PNG 转换走延迟 import，不强制装 PySide6。等 UI 真正实施时再装到 F 盘 venv。
- **L5 接近上限**：L5 多色渐变 + 滤镜 + 电路节点，代码 80 行（非预期 150 行，因 group+复用），生成时间 0.05ms，文件 2.13 KB。仍可读但接近上限——后续若图标更复杂需拆 sub-primitive。
- **测试产物不入 git**：5 个测试 SVG 验证完上限后用 `--clean` 删除，icons/ 只留 .gitkeep，等 UI 真正需要图标时再生成正式图标。避免文件杂乱臃肿。
- **Lucide path 许可**：Lucide 是 ISC 许可（免费商用），可直接采用其 path data。但 L4 cloud-sync 和 L5 ai-brain 是组合/原创设计，非 Lucide 官方图标。

## 六、执行记录

- 执行时间：2026-06-19 19:57
- 实际改动：与方案一致，含一次 bug 修复（SVG 属性名）+ 一次细节优化（参考 Lucide 风格）
- 验证结果：通过
  - 5 个 SVG 全部生成成功 + XML well-formed
  - 工具链全绿：ruff check / ruff format / mypy strict
  - 测试产物已清理（icons/ 只剩 .gitkeep）
- 后续遗留：
  - PyInstaller 安装 + 装 PySide6（待 UI 框架阶段）
  - UI 界面实施（PySide6 主窗口骨架）
  - 调 /ui-ux-pro-max skill 产设计系统规范 → 调整脚本默认参数
  - 打 v0.0.1 .exe（含 UI 空壳）到 安装包/v0.0.1/