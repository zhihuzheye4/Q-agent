<!--
权限：L1 可写不可读
说明：本方案由 AI 在与用户讨论后生成。AI 执行时不回读本文件，避免被旧方案锚定。
     人类可查阅作为审计/历史。执行完毕后归档到 memory/方案/已执行/。
-->

# 方案：UI 界面开发（活的 UI 空壳 + 方案 D）

- 生成时间：2026-06-19 20:10
- 触发需求：用户三步指令——(1) 修正 ADR-014 为方案 D (2) 用方案 D 执行 UI 设计 (3) 开发活的 UI 界面（界面可跳转、按钮可按，无实际功能）
- 讨论结论：用 /ui-ux-pro-max skill 产设计系统规范 → 用方案 D（QIcon 直接受 SVG）实施 q_agent/ui/ 骨架 → PyInstaller 打 v0.0.1 .exe
- 状态：待执行

## 一、要做什么

1. ADR-016 已追加（取代 ADR-014 方案 C 部分，改方案 D）
2. 装 PySide6 + PyInstaller 到 F 盘 venv，更新 pyproject.toml dev 依赖
3. 调 /ui-ux-pro-max skill --design-system 产设计规范，保存 memory/设计规范/MASTER.md
4. 生成 UI 图标：追加 ICONS 定义到 scripts/generate_icons.py，跑脚本生成 chat/skills/memory/settings/send/new-chat/clear/about 等 SVG + manifest.json
5. 实施 q_agent/ui/ 模块：
   - icons.py（方案 D 核心：QIcon 直接受 SVG + manifest 索引）
   - theme.py（QSS 从设计规范翻译）
   - main_window.py + sidebar.py + toolbar.py + menu_bar.py
   - pages/chat_page.py + skills_page.py + memory_page.py + settings_page.py
6. CLI 接入 ui 子命令（延迟 import PySide6）
7. 写测试 + 工具链全绿（ruff + mypy + pytest）
8. 手动测试 UI：4 tab 切换 + 输入框/发送按钮 echo + 设置面板切换 + 工具栏按钮 + 菜单栏
9. 打 v0.0.1 .exe：PyInstaller --onefile --windowed --add-data，重命名到 安装包/v0.0.1/
10. 验证 .exe 零外部依赖运行
11. 记忆同步（临时记忆覆写前归档编年史 + 项目指导文件 + 技能工具注册清单 + 思维导图 + 方案归档）
12. commit + git push（3 次 commit 待推：cba9d0d + 862a6ea + 3d44456 + 本次）

## 二、涉及的文件

### 新增
- `q_agent/ui/__init__.py`、`main_window.py`、`sidebar.py`、`toolbar.py`、`menu_bar.py`、`theme.py`、`icons.py`
- `q_agent/ui/pages/__init__.py`、`chat_page.py`、`skills_page.py`、`memory_page.py`、`settings_page.py`
- `q_agent/assets/icons/chat-active.svg`、`skills-active.svg`、`memory-active.svg`、`new-chat-active.svg`、`clear-active.svg`、`about-active.svg`、`manifest.json`
- `memory/设计规范/MASTER.md`
- `memory/方案/已执行/已执行_方案_2026-06-19_2010_UI界面开发.md`（本方案归档）
- `tests/test_ui_imports.py`、`tests/test_icons.py`
- `安装包/v0.0.1/Q-agent.exe`（不入 git）

### 修改
- `pyproject.toml`（dev 依赖加 PySide6 + PyInstaller + mypy overrides）
- `q_agent/cli.py`（新增 ui 子命令）
- `scripts/generate_icons.py`（追加 UI 图标定义）
- `memory/临时记忆.md`（覆写前归档编年史）
- `memory/项目指导文件.md`（更新当前节点 + 已完成节点）
- `memory/技能工具注册清单.md`（注册 ui 模块 + 新增图标）
- `memory/思维导图数据.jsonl` + `memory/思维导图.md`（追加 UI 节点）
- `安装包/README.md`（版本历史表追加 v0.0.1）

## 三、实施步骤

按计划文件第 14 步序列执行。

## 四、验证方法

- 工具链全绿：ruff check / ruff format --check / mypy q_agent / pytest -q
- UI 手动测试：4 tab 切换 + 发送按钮 echo + 设置面板切换 + 工具栏按钮 + 菜单栏弹窗
- .exe 验证：双击运行零外部依赖
- 方案 D 验证：icons.py 用 QIcon(svg_path) 不写预渲染层

## 五、风险与边界

- PySide6 装 F 盘 venv 可能网络失败 → SVG-only 模式已验证，UI 实施阶段必须装成功
- mypy strict 对 PySide6 报错 → pyproject.toml mypy overrides 加 ignore_missing_imports
- QSS 不支持 CSS 全部特性 → box-shadow 用 QGraphicsDropShadowEffect，CSS 变量手动翻译成 QSS 常量
- 不实现实际功能（LLM/技能/记忆后端）→ UI 是空壳，所有交互只前端响应
- 不实现设置持久化、快捷键、主题切换（留待下一里程碑）

## 六、执行记录

- 执行时间：2026-06-19 20:10 ~ 23:00
- 实际改动：
  1. ADR-016 追加（取代 ADR-014 方案 C 部分）
  2. PySide6 6.11.1 + PyInstaller 6.21.0 装到 F 盘 venv + 加入 pyproject.toml dev 依赖 + mypy overrides
  3. 调 ui-ux-pro-max skill --design-system -f markdown 产设计规范，存 memory/设计规范/MASTER.md
  4. 追加 6 个 UI 图标定义到 scripts/generate_icons.py（chat/skills/memory/new-chat/clear/about）+ 跑脚本生成 11 SVG + manifest.json
  5. 实施 q_agent/ui/ 模块（9 个 .py）+ 方案 D 实现
  6. cli.py 新增 ui 子命令 + 修复缺 `if __name__ == "__main__":` 守护关键 bug
  7. 写 tests/test_ui_imports.py 11 测试
  8. PyInstaller 打 v0.0.1 .exe 到 安装包/v0.0.1/Q-agent.exe（47MB）
- 验证结果：通过
  - 工具链全绿：ruff check + ruff format --check + mypy strict + pytest -q（31 测试，覆盖率 84.82%）
  - UI 启动测试通过：4 tab 切换 + 输入框启用 + echo 消息追加
  - .exe 验证：Start-Process 启动 UI 进程持续运行（PID 14004），零外部依赖
- 后续遗留：
  - 接入本地 LLM（Ollama / llama.cpp）→ 对话 tab 真调 LLM
  - 设置持久化（设置 tab 状态写 ~/.qagent/settings.json）
  - 快捷键系统（Ctrl+K command palette）
  - 字体优化（装 Inter 字体到 q_agent/assets/fonts/）
  - 多窗口 / 标签页（多会话并行）
  - 调 ui-ux-pro-max --persist --page 生成各页 override（可选）