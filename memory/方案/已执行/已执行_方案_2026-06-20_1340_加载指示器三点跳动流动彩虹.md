# 已执行方案：加载指示器（三点跳动 + 流动彩虹 + 半透明）

执行时间：2026-06-20 13:40
版本：v0.0.10
对应 commit：待 commit

## 一、用户需求

> 添加一个进度条，或正在加载显示，彩色+高透明度

## 二、背景

v0.0.8 接通 Ollama 流式后，用户发送对话到首个 chunk 到达之间有数百毫秒~数秒"空等期"，期间 UI 只显示"生成中"按钮文字，pending AI 气泡为空 QLabel（高度 0 看不见）。用户看不出"AI 是否在跑"，体验空洞。v0.0.10 加加载指示器填补空等期。

## 三、UX 决策（AskUserQuestion 答复）

| 维度 | 选项 | 决策 |
|------|------|------|
| 形式 | 不确定进度条 / spinner / 三点跳动 / 流动彩虹横条 | **三点跳动**（iMessage 打字气泡风格，轻量友好） |
| 位置 | pending 气泡内 / 按钮位置 / 顶部悬浮条 / 输入框上方 | **pending AI 气泡内**（贴近用户视线） |
| 彩色 | 模型 hash 色 / 流动彩虹渐变 / 品牌主色 | **流动彩虹渐变**（HSV 色相循环，独立于模型色，活泼） |
| 透明度 | 元素半透明 / 消息流遮罩 / 气泡半透明 | **加载指示元素本身半透明**（alpha=180 ≈ 70%，不刺眼） |

## 四、实现

### 新建 q_agent/ui/loading_dots.py

- `LoadingDots(QWidget)` 自定义 `paintEvent`
- 三个圆点（DOT_SIZE=8, DOT_GAP=8）依次上下跳动（DOT_BOUNCE=5px）
- 相位偏移 `i * 0.33` 让三个点错开 1/3 周期 → 依次跳动
- 跳动用 `-abs(math.sin(t * 2π))` 平滑上跳下落
- 颜色 `QColor.fromHsv((phase*360 + i*60) % 360, 220, 230, 180)` → 流动彩虹
- QTimer 80ms tick，phase += 0.05，1.6s 一周期
- 父 widget 销毁时 QTimer 自动停止（Qt parent 机制）

### 集成 q_agent/ui/pages/chat_page.py

- 新字段 `_pending_loading: LoadingDots | None`
- `_add_message` 加 `loading: bool = False` 参数，pending AI 气泡用 True → 在模型名与气泡之间插入 LoadingDots
- 新增 `_remove_pending_loading()` 辅助方法（hide + deleteLater + 清引用）
- `_on_chunk` 首次调用（_pending_loading 非 None）→ 移除 LoadingDots（回复开始流入）
- `_on_chat_failed` / `_on_chat_done` / `_clear_messages` 均清理 LoadingDots
- import `from q_agent.ui.loading_dots import LoadingDots`

## 五、测试

### 新建 tests/test_ui_loading_dots.py（5 例）

- test_loading_dots_constructs_with_fixed_size（构造 + 固定尺寸）
- test_loading_dots_timer_ticks_advance_phase（QTimer 启动 + tick 推进 phase）
- test_loading_dots_paint_does_not_crash（paintEvent 渲染不崩）
- test_loading_dots_first_dot_phase_offset_zero（多 phase 值渲染不崩）
- test_loading_dots_cleanup_on_parent_destroy（parent 销毁跟随）

### tests/test_ui_chat_page.py 追加 5 例集成测试

- test_loading_dots_created_on_send_clicked（_on_send_clicked 后 _pending_loading 非 None）
- test_loading_dots_removed_on_first_chunk（首次 chunk 移除）
- test_loading_dots_removed_on_chat_failed
- test_loading_dots_removed_on_chat_done_empty_reply
- test_clear_messages_clears_pending_loading_ref

## 六、工具链全绿

- ruff format + ruff check
- mypy strict
- pytest -q：130 测试通过，覆盖率 86.00%

## 七、版本升级

- __version__ 0.0.9 → 0.0.10
- v0.0.10 .exe 打包：47MB 零外部依赖，PyInstaller --onefile --windowed --clean
- 删除 v0.0.9 子目录，仅留 v0.0.10（按"只留最新"规则）
- 安装包/README.md 版本历史表追加 v0.0.10 行
- ADR-024 追加

## 八、设计取舍

- **不用 QPropertyAnimation + QML**：自定义 paintEvent + QTimer 更轻量，PyInstaller 打包友好（不引入 QtQml 依赖），符合 ADR-015 零新运行时依赖
- **不用 QMovie/GIF**：GIF 资源需打包进 .exe 增体积，矢量代码渲染更轻
- **HSV 而非固定调色板**：流动效果需要色相连续变化，HSV 是最自然的色彩空间
- **透明度 alpha=180 而非 setStyleSheet opacity**：Qt widget 无原生 opacity 属性，QGraphicsEffect 有性能开销，直接 painter alpha 通道最直接
- **跳动幅度 5px 而非更大**：视觉提示性足够 + 不抢眼不晃动，符合"半透明不刺眼"决策
- **流动彩虹独立于模型色**：与 v0.0.9 模型名 hash 色并行（不冲突）——加载状态是临时态，模型色是持久态，色彩维度解耦更清晰

## 九、未解决问题

- LoadingDots 固定 dot_size=8 不随 DPI 缩放（高 DPI 屏可能偏小）—— v0.0.11+ 候选
- 加载时长无统计（如 5s 无 chunk 可提示"Ollama 响应慢"）—— v0.0.11+ 候选
- 取消按钮未接（加载中无法点取消），ChatWorker.stop() 接口已留 —— v0.0.11+ 候选