<!--
权限：L1 可写不可读
说明：本方案由 AI 在与用户讨论后生成。AI 执行时不回读本文件，避免被旧方案锚定。
     人类可查阅作为审计/历史。执行完毕后归档到 memory/方案/已执行/。
-->

# 方案：项目框架搭建

- 生成时间：2026-06-19 13:30
- 触发需求：用户提出"搭建项目框架以及开发执行规则"，并明确框架=项目开始前的准备工作
- 讨论结论：框架阶段一次性配齐工具链+核心模块骨架+项目指导文件+方案文件夹机制+开发执行规则；永不沙箱；增量开发哲学
- 状态：已完成

## 一、要做什么

搭建 Q-agent 项目框架，作为"类似 Claude Code 的桌面端 AI 工具"的开发起点：
- 工具链配齐（ruff/mypy/pytest）
- 核心模块骨架就位（cli/orchestrator/llm/tools/skills/memory 全 stub）
- 项目指导文件（Claude Code 导航图）
- 方案文件夹机制（每次讨论后方案存 L1 不可读）
- 开发执行规则写入 CLAUDE.md

## 二、涉及的文件

### 新增
- `G:\agent\pyproject.toml`
- `G:\agent\q_agent\__init__.py` / `__main__.py` / `cli.py`
- `G:\agent\q_agent\skills\` 5 个文件（base/registry/builtin 2/__init__）
- `G:\agent\q_agent\memory\` 3 个文件（base/runtime/__init__）
- `G:\agent\q_agent\orchestrator\` 3 个文件（core/planner/__init__）
- `G:\agent\q_agent\llm\` 3 个文件（base/local/__init__）
- `G:\agent\q_agent\tools\` 5 个文件（base/registry/safety/executor/__init__）
- `G:\agent\tests\` 5 个文件（__init__/conftest/test_smoke/test_registry/test_safety）
- `G:\agent\README.md` / `.pre-commit-config.yaml`
- `G:\agent\memory\项目指导文件.md`（L3）
- `G:\agent\memory\方案\` + `已执行\` 目录

### 修改
- `G:\agent\CLAUDE.md`：概况更新 + 记忆文件清单追加方案文件夹和项目指导文件 + 末尾追加「开发执行规则」16 节
- `G:\agent\memory\技能工具注册清单.md`：注册 echo 技能
- `G:\agent\memory\思维导图数据.jsonl`：追加 3 个事件 + 修复粘连 bug
- `G:\agent\memory\思维导图.md`：脚本重新渲染
- `G:\agent\memory\ADR架构决策记录.md`：追加 ADR-005 到 ADR-009
- `G:\agent\memory\编年史记忆.md`：归档旧临时记忆快照
- `G:\agent\memory\临时记忆.md`：覆盖更新当前状态

## 三、实施步骤

1. 创建 pyproject.toml + q_agent 顶层包文件
2. 创建 skills 包（含 echo 内置技能）
3. 创建 memory/orchestrator/llm/tools 包（共 14 文件）
4. 创建 tests + README + .pre-commit-config
5. 创建 memory/方案/ 目录结构
6. 创建 memory/项目指导文件.md
7. 更新 CLAUDE.md（追加开发执行规则 + 调整记忆文件清单）
8. 用 Python 脚本一次性 append L1 文件（编年史归档 + 思维导图数据 + ADR）
9. 修复思维导图 jsonl 粘连 bug
10. 调脚本重新渲染思维导图
11. 更新技能工具注册清单（L3）
12. 覆盖临时记忆（L3，编年史已归档）
13. 生成本方案文件并归档
14. pip install + 工具链验证 + commit

## 四、验证方法

1. `python -m q_agent version` → 输出 `Q-agent 0.0.1`
2. `python -m q_agent chat "echo 你好"` → 输出 `你好`
3. `python -m q_agent skills` → 列出 `echo` 技能
4. `ruff check . && ruff format --check .` → 全绿
5. `mypy q_agent` → 全绿
6. `pytest -q` → 全绿，覆盖率 ≥ 30%
7. `memory/项目指导文件.md` 存在且结构正确
8. `memory/方案/已执行/` 含本方案归档

## 五、风险与边界

1. **C 盘污染**：pip install 用 F 盘 target
2. **方案文件被 AI 误读**：CLAUDE.md 明示 AI 不读方案文件
3. **subprocess 在 Windows 兼容性**：shell=False + list 参数
4. **ruff/mypy 误报**：骨架阶段签名简洁，若出现先调规则集

## 六、执行记录（执行后追加）

- 执行时间：2026-06-19 13:30-14:00
- 实际改动：与方案一致；额外修复了思维导图 jsonl 粘连 bug（第一行末尾缺换行符）
- 验证结果：待执行 pip install + 工具链验证后补充
- 后续遗留：等用户提下一个具体功能需求