# 已执行方案：硬件监控曲线（sidebar 底部常驻 4 折线 60s 历史）

执行时间：2026-06-20 15:50
版本：v0.0.12
对应 commit：待 commit

## 一、用户需求

> 增加下一功能，在左侧工具栏，增加硬件占用曲线，CPU和GPU，实时显示

## 二、用户确认维度（AskUserQuestion 4 个回答）

1. **位置**：sidebar 底部常驻（不是单独窗口/工具栏底部）
2. **指标**：CPU% + 显存% + 内存 RAM% + GPU 利用率%（4 个全部上）
3. **依赖**：引入 psutil + pynvml（nvidia-ml-py）运行时依赖，PyInstaller 打包进 .exe（符合 ADR-015）
4. **形式**：折线图历史 60s（不是数字仪表盘/柱状图）

## 三、实现

### 代码改动

#### 新建 `q_agent/ui/hardware_monitor.py`

- `HardwareMonitor(QWidget)` 自定义 `paintEvent`：
  - 顶部 20px 图例（4 个色块 + 名称 + 当前数值，无数据时显示 N/A）
  - 下方 140px 折线图区域（4 条折线叠加，0-100% y 轴，25%/50%/75% 网格线）
  - 背景填充 #0F172A 与 sidebar 同色
  - None 段断开不连线（避免错误跨断点连线）
- `HardwareMonitorWorker(QThread)`：
  - `sample_collected = Signal(object)` 信号
  - `__init__` 调 `pynvml.nvmlInit()` 容错，失败时 `_nvml_ok=False`
  - `run()` 先 `psutil.cpu_percent(interval=0.1)` 预热基线（第一次调用返回 0），再循环采集
  - `_collect()` 返回 `dict[str, float | None]`，4 字段
  - `stop()` 设 `_stop` flag + `contextlib.suppress(Exception)` 调 `nvmlShutdown`
- 4 指标 4 色：CPU 蓝 #3B82F6 / GPU 绿 #22C55E / VRAM 紫 #A855F7 / RAM 橙 #F97316
- 灰色 N/A 占位 #475569
- 常量：`HISTORY_SECONDS=60`、`SAMPLE_INTERVAL_MS=1000`、`MONITOR_HEIGHT=160`
- `collect_sample_sync()` 测试辅助：不依赖 QThread 同步采集一个样本

#### 改造 `q_agent/ui/sidebar.py`

- 从 `QListWidget` 直接子类化改为 `QFrame` 容器
- QVBoxLayout：上方 `QListWidget`（tab 列表，stretch=1）+ 下方 `HardwareMonitor`（常驻）
- 保留 `tab_changed(int)` 信号、`SIDEBAR_WIDTH=200`、4 tab 元数据
- `hardware_monitor` 作为公开属性供 MainWindow 调用 `start()` / `stop()`

#### 改造 `q_agent/ui/main_window.py`

- `QTimer.singleShot(200, self.sidebar.hardware_monitor.start)` 启动 worker（延迟 200ms 让 UI 先绘制）
- `closeEvent(event: QCloseEvent)` 调 `worker.stop()` + `wait(2000)` 优雅退出
- import `QCloseEvent` 替代 `# noqa: ANN001`（mypy strict 要求类型注解）

### 测试

#### 新建 `tests/test_ui_hardware_monitor.py`（10 例）

- `test_hardware_monitor_constructs_with_fixed_height`：构造 + 固定高度 + worker 子组件
- `test_on_sample_appends_and_truncates_history`：追加 + 截断到 60s
- `test_on_sample_none_does_not_crash`：None 段不崩
- `test_paint_event_renders_without_crash_empty_history`：空历史不崩
- `test_paint_event_renders_with_partial_data`：部分数据不崩
- `test_paint_event_renders_with_full_data`：全数据不崩
- `test_draw_line_none_segment_breaks_line`：None 段断开
- `test_draw_line_all_none_draws_na_placeholder`：全 None 画 N/A 占位
- `test_collect_sample_sync_returns_4_fields_dict`：返回 4 字段
- `test_worker_init_handles_no_pynvml`：无 pynvml 不崩

#### 改写 `tests/test_ui_imports.py` 2 例

- `test_main_window_constructs`：`w.sidebar.count()` → `w.sidebar._list.count()` + 验证 hardware_monitor 子组件
- `test_sidebar_tab_switches_stack`：`w.sidebar.setCurrentRow(i)` → `w.sidebar._list.setCurrentRow(i)`

## 四、工具链全绿

- ruff format + ruff check：全绿
- mypy strict：全绿（pynvml / psutil 加 `# type: ignore[import-untyped]`，collect_sample_sync 内的 type: ignore 被认为多余已移除）
- pytest -q：144 测试通过，覆盖率 84.24%

## 五、版本升级

- __version__ 0.0.11 → 0.0.12

## 六、.exe 打包

- 命令：`PyInstaller --distpath 安装包 --workpath build --name Q-agent --onefile --windowed --clean --hidden-import pynvml --collect-all pynvml --collect-all psutil q_agent/cli.py`
- 产物：47MB 零外部依赖
- 删除 v0.0.11 子目录，仅留 v0.0.12
- 启动验证：`Q-agent.exe version` → `Q-agent 0.0.12`

## 七、记忆同步

- ADR-026 追加（硬件监控曲线决策）
- 思维导图数据.jsonl 追加一行 → update_mindmap.py 重新渲染（21 存活功能）
- 技能工具注册清单追加 HardwareMonitor / HardwareMonitorWorker / Sidebar 容器化改造条目
- 项目指导文件追加 v0.0.12 进度
- 安装包/README.md 版本历史表追加 v0.0.12 行
- 临时记忆备份编年史 + 重写为 v0.0.12 完成版

## 八、设计取舍

- **折线图而非柱状图**：折线展示 60s 历史趋势更直观（用户能看出"5s 前飙升过一次"），柱状图只能看当前瞬间
- **4 指标叠加而非分 4 个子图**：sidebar 宽度仅 200px，分 4 个子图每个太窄看不清；叠加在同一坐标系 + 4 色区分更紧凑
- **60s 历史**：足够看趋势，又不至于太长折线挤成一团。1s 一个样本 × 60 = 60 个点，分辨率刚好
- **psutil + pynvml 而非 GPUtil**：GPUtil 内部也是调 pynvml，多一层封装反而不透明；直接用 pynvml 更可控，且能拿显存（GPUtil 不直接支持）
- **pynvml import 失败容错而非硬依赖**：让无 NVIDIA 显卡的用户也能用 CPU + RAM 折线，不会因 GPU 不可用就整个组件崩溃
- **`contextlib.suppress(Exception)` 替代 `try-except-pass`**：ruff 规则要求，更 Pythonic
- **`--collect-all pynvml` 打包**：pynvml 内部有动态 import 路径（nvidia-ml-py 包名 vs pynvml 模块名），`--collect-all` 确保 .exe 内部能正确找到
- **worker.stop() + wait(2000)**：先设 flag 让 run 循环早退，再 wait 最多 2s 确保线程真正退出；超时 2s 足够 1s 采样间隔的 msleep 早退
- **延迟 200ms 启动 worker**：让 UI 先绘制避免首帧空白

## 九、未解决问题

- 采样间隔固定 1s 不可配置（未来可在设置中调 1s/2s/5s）
- 历史窗口固定 60s 不可配置（未来可调 60s/120s/300s）
- 不写盘持久化（关闭重启后历史清空）
- 非 NVIDIA GPU 不支持（AMD/Intel 需引入额外依赖）
- 无报警阈值（如 CPU > 90% 持续 10s 红色高亮）
- 无单指标点击放大查看（小屏空间不够，需新窗口）