<!--
权限：L1 可写不可读
说明：本方案由 AI 在与用户讨论后生成。AI 执行时不回读本文件，避免被旧方案锚定。
     人类可查阅作为审计/历史。执行完毕后归档到 memory/方案/已执行/。
-->

# 方案：新规则制定（安装包机制 + UI 矢量图资源隔离 + 指令执行自由度）

- 生成时间：2026-06-19 17:55
- 触发需求：用户提出三部分新规则
- 讨论结论：全部采纳，按用户字面要求执行
- 状态：已完成

## 一、要做什么

用户提出三部分新规则：

1. **安装包文件夹**：新建文件夹，每完成一部分工作必须形成可执行 .exe 放到该文件夹内
2. **UI 矢量图文件夹**：UI 界面调用的所有矢量图存到单独文件夹，只存 UI 调用的矢量图
3. **全局记忆**：记录"除非有明确指令，否则用户下达的指令都是有自由选择空间的，非强制执行"

## 二、涉及的文件

### 新增
- `G:\agent\安装包\.gitkeep` + `G:\agent\安装包\README.md`
- `G:\agent\q_agent\assets\README.md` + `G:\agent\q_agent\assets\icons\.gitkeep`
- `C:\Users\sutai\.claude\projects\G--agent\memory\feedback_command_interpretation.md`（全局记忆）
- `G:\agent\memory\方案\已执行\已执行_方案_2026-06-19_1755_新规则制定.md`（本方案归档）

### 修改
- `G:\agent\.gitignore`（加 `安装包/*` + 例外保留 .gitkeep/README）
- `G:\agent\CLAUDE.md`（追加第十七/十八/十九节，含 Python 版本号修正 3.11→3.10）
- `G:\agent\项目结构说明.html`（加安装包/ + q_agent/assets/ 节点）
- `G:\agent\memory\项目指导文件.md`（更新当前节点 + 已完成节点）
- `G:\agent\memory\临时记忆.md`（更新当前进度 + 下一步 + ADR 列表）
- `G:\agent\memory\ADR架构决策记录.md`（追加 ADR-010/011/012）
- `G:\agent\memory\思维导图数据.jsonl`（追加 3 条事件）
- `G:\agent\memory\思维导图.md`（脚本重新渲染）
- `C:\Users\sutai\.claude\projects\G--agent\memory\MEMORY.md`（加 feedback 索引）

## 三、实施步骤

1. 写全局记忆 feedback_command_interpretation.md + 更新 MEMORY.md 索引
2. 建 G:\agent\安装包\ + .gitkeep + README（含版本历史表）
3. 建 G:\agent\q_agent\assets\icons\ + .gitkeep + assets/README
4. .gitignore 加 `安装包/*` + 例外 `!安装包/.gitkeep` + `!安装包/README.md`
5. CLAUDE.md 追加三节规则（第十七/十八/十九）+ 修正 Python 版本号
6. ADR 追加 ADR-010/011/012
7. 思维导图 jsonl 追加 3 条 + 跑 update_mindmap.py 渲染
8. 项目指导文件 + 临时记忆更新当前节点
9. HTML 加新文件夹节点
10. 工具链全绿验证：ruff + mypy + pytest
11. commit 862a6ea

## 四、验证方法

- `git check-ignore -v "安装包/.gitkeep" "安装包/README.md" "安装包/v0.0.1/Q-agent.exe"` 三个路径分别命中例外规则、例外规则、忽略规则
- `cat G:\agent\安装包\README.md` 含版本历史表
- `cat G:\agent\q_agent\assets\icons\.gitkeep` 空文件占位
- CLAUDE.md 含第十七/十八/十九节
- 全局记忆 MEMORY.md 含 feedback_command_interpretation 索引
- 思维导图 md 含新增 3 个节点
- ruff check + ruff format + mypy strict + pytest 20 passed 全绿

## 五、风险与边界

- **PyInstaller 未装**：规则写明每里程碑打 .exe，但 v0.0.1 第一个 .exe 待用户确认后生成。原因：PyInstaller 是大依赖（约 50MB），且当前框架阶段功能仅 echo，打包价值有限。等用户确认时机。
- **.exe 不入 git**：二进制大文件入 git 会膨胀仓库，用 .gitignore 例外保留目录结构。历史版本长期保留在本地，未来可用 GitHub Releases 分发。
- **UI 矢量图资源**：当前为空目录，等 UI 界面实现时填充。包内资源设计意味着 PyInstaller 打包时自动随包分发，无需额外配置。
- **指令执行自由度**：元规则影响后续所有交互。大决策仍需先展示方案让用户否决，避免 AI 自作主张做不可逆操作。

## 六、执行记录

- 执行时间：2026-06-19 17:35~17:55
- 实际改动：与方案完全一致，无偏差
- 验证结果：通过
  - git check-ignore 三个路径规则命中符合预期
  - 工具链全绿：ruff check + ruff format + mypy strict + pytest 20 passed + 覆盖率 61.7%
  - commit 862a6ea 成功，12 文件变更 +157 -11
- 后续遗留：
  - PyInstaller 安装 + 打 v0.0.1 .exe（待用户确认时机）
  - git push（待网络允许，commit cba9d0d + 862a6ea 两次待推）