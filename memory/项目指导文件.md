<!--
权限：L3 完全权限
说明：本文件是 Claude Code 在 Q-agent 项目工作时的导航文件。
     不是一次性实施方案，而是项目方向 + 各模块准备情况 + 下一步可能选项的导航图。
     "一开始就需要的"已就位；"准备好但目前用不上的"在下方标注，走到那步再调用。
     具体执行方案存到 memory/方案/（L1 不可读）。
-->

# Q-agent 项目指导文件

## 项目最终形态

类似 Claude Code 的桌面端 AI 工具：
- 纯软件形式（不是 Agent 框架 SDK）
- 对接本地 LLM 大模型
- 让本地 LLM 发出的指令被软件理解并执行
- 实现各大 AI 工具可实现的功能（对话、工具调用、技能学习、记忆等）

## 关键约束

- **永不沙箱**：本项目不引入沙箱模块。LLM 指令通过工具调用层直接执行，仅保留基本输入校验（危险命令黑名单 + 项目根目录保护）
- **本地 LLM 优先**：默认走本地后端，云端按需启用
- **零第三方依赖起步**：CLI 用 argparse，不引入 Typer/Click
- **分发零外部依赖硬规则**：最终用户拿 `.exe` 双击即跑，禁止让用户装任何外部依赖。所有运行时依赖由 PyInstaller `--onefile` 打包进单个 `.exe`（详见 CLAUDE.md 第二十节 + ADR-015）

## 技术栈

- 语言：Python 3.10+
- 包管理：pip + pyproject.toml（不引入 poetry/uv）
- 工具链：ruff（lint+format）+ mypy strict + pytest + pytest-cov
- 测试覆盖率：起步 30%，渐进到 70%

## 核心模块（一开始就需要的，已就位 stub）

| 模块 | 路径 | 状态 | 说明 |
|------|------|------|------|
| CLI 入口 | q_agent/cli.py | stub | argparse 子命令分发 |
| 编排核心 | q_agent/orchestrator/ | stub | Orchestrator + Planner |
| LLM 抽象 | q_agent/llm/ | stub | LLMClient 基类 + 本地后端占位 |
| 工具调用层 | q_agent/tools/ | stub | @tool 装饰器 + 执行器 + 基本安全 |
| 技能体系 | q_agent/skills/ | stub | @skill 装饰器 + echo 内置 |
| 运行期记忆 | q_agent/memory/ | stub | MemoryStore + RuntimeMemory |

## 准备好但目前用不上的（走到那步再调用）

> 以下方向在框架阶段不实现，等用户提具体需求时再增量添加。

### 多 Agent 协作
- 子 Agent 与主 Agent 同时唤醒
- Agent 间通信协议
- 触发条件：用户提出"多 Agent 协作"需求

### 知识库 / RAG
- 向量化记忆
- 文档检索增强
- 触发条件：用户提出"知识库"或"RAG"需求

### 插件机制
- 第三方插件加载
- 插件市场（用户已明确"不搞插件市场"）
- 触发条件：用户提出"插件"需求

### 云端 LLM 后端
- Claude / OpenAI / Gemini 接入
- 触发条件：用户提出"接入云端 LLM"需求

### GUI 界面
- 桌面端 UI（PyQt / Tkinter / Web UI）
- 触发条件：用户提出"UI 界面"需求

### 文件操作工具集
- read/write/move/search 等文件工具
- 触发条件：用户提出"文件操作"需求

### 网络工具集
- HTTP 请求、网页抓取、API 调用
- 触发条件：用户提出"网络工具"需求

### 代码执行工具
- Python 代码沙箱执行（注意：这是工具调用层的一个工具，不是项目级沙箱）
- 触发条件：用户提出"代码执行"需求

## 开发流程（增量盖房）

1. 用户提出一个具体需求
2. 讨论（可能多轮）→ 达成共识
3. AI 生成具体可执行方案 → 写入 `memory/方案/方案_YYYY-MM-DD_HHmm_主题.md`（L1 不可读）
4. 用户审阅方案
5. 用户确认后 → AI 执行（基于当前会话上下文，不回读方案文件）
6. 执行完毕 → 方案归档到 `memory/方案/已执行/`
7. 更新本文件（当前节点 + 已完成节点）

## 当前节点

- 日期：2026-06-20
- 阶段：v0.0.14 完成（贴纸式重构治本回退 v0.0.12 sidebar 容器化错误）
- 已完成：记忆系统 + 项目框架 + 项目指导文件 + 方案文件夹机制 + safety.py 审查修复 + 安装包/UI 矢量图/指令执行自由度/贴纸式开发原则规则 + 分发零外部依赖硬规则 + 矢量图生成脚本 + ADR-016~028 + 设计系统规范 + UI 图标生成 + q_agent/ui/ 模块 + 144 测试全绿 + v0.0.14 .exe（零外部依赖，仅留最新版） + Ollama 模型检测 + 下拉框本地/云端分组 + AI 气泡模型名标签 + llm 层对称骨架 + Ollama Cloud 转发模型分类 + 通过 Ollama 唤醒模型 + 流式批量刷新 + 模型内存释放 + 切换模型清空上下文 + 模型名 hash 颜色 + 加载指示器 + release_model 卸载验证 + 硬件监控曲线（sidebar 恢复 QListWidget 子类 + MainWindow left panel 贴纸式挂载 HardwareMonitor + psutil/pynvml 运行时依赖） + 贴纸式开发原则明文化（CLAUDE.md 第二十一节） + 打包命令固化（CLAUDE.md 第十七节补充 + 严格从 README.md 复制粘贴）
- 进行中：等待用户验证 v0.0.14 .exe 双击效果（tab 大小/图标/tooltip/硬件监控曲线），然后提下一个需求
- 下一步：由用户决定（候选：取消按钮接 ChatWorker.stop() / 设置持久化 QSettings / 多轮上下文裁剪 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接 OpenAI/Anthropic/Google API / 历史消息窗口策略 / 释放成功后下拉框不选中此模型 / 新建对话按钮实际行为 / 清空按钮实际行为 / 加载时长统计 + 慢响应警告 / 硬件监控采样间隔可配置 / 硬件监控历史窗口可配置 / 硬件监控报警阈值）——**所有新功能必须遵循贴纸式开发原则（CLAUDE.md 第二十一节）：独立 widget + 父容器一行挂载 + 既有模块零改动**

## 已完成节点

- [2026-06-18] 项目初始化（CLAUDE.md + .gitignore）
- [2026-06-19] 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- [2026-06-19] 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- [2026-06-19] 独立审查修复（safety.py 路径前缀归属 + 命令 token 化匹配 + .gitignore 补全 + 测试覆盖盲区）
- [2026-06-19] 新增规则：安装包文件夹 + UI 矢量图文件夹 + 指令执行自由度
- [2026-06-19] 新增硬规则：分发零外部依赖（CLAUDE.md 第二十节）+ 矢量图生成脚本（scripts/generate_icons.py，5 级递进测试通过，参考 Lucide 风格优化，测试产物已清理）
- [2026-06-19] ADR-016 追加（取代 ADR-014 方案 C，改方案 D：QIcon 直接受 SVG + Qt 内部按 size 智能缓存）
- [2026-06-19] 设计系统规范产出（调 /ui-ux-pro-max --design-system，存 memory/设计规范/MASTER.md，Dark Mode OLED + 10 color token + Inter 字体）
- [2026-06-19] UI 图标生成（6 个 UI 用 + 5 个测试样本，11 SVG + manifest.json 全 well-formed）
- [2026-06-19] q_agent/ui/ 模块实施（方案 D：icons/theme/main_window/sidebar/toolbar/menu_bar/pages，活 UI 空壳 4 tab 切换 + 输入框/发送按钮 echo + 设置面板 + 工具栏 + 菜单栏）
- [2026-06-19] 测试与工具链全绿（11 UI 测试 + 31 总测试，覆盖率 84.82%，ruff + mypy strict 全绿）
- [2026-06-19] PyInstaller 打 v0.0.1 .exe（47MB，零外部依赖，--onefile --windowed --add-data）
- [2026-06-19] v0.0.1 双击崩溃修复（cli.py main() 默认 argv=["ui"]，--windowed 模式 argparse 不再写 None.stderr）
- [2026-06-19] UI 改进第一轮 v0.0.2（tooltip 透明背景 + 气泡宽度 0.7→0.92 + Feather 齿轮 + 输入框 QLineEdit→QTextEdit 多行）
- [2026-06-19] UI 改进第二轮 v0.0.3（输入框动态高度，documentSizeChanged 信号 + [44,200] 钳制 setFixedHeight，触顶滚动条）
- [2026-06-19] ADR-017 追加（UI 输入框动态高度策略）
- [2026-06-20] 第一个填充实际功能：UI 右上角模型下拉框 + 启动检测本地 Ollama（q_agent/llm/ollama.py list_models，urllib 标准库零新依赖，ModelRefreshWorker QThread 异步，失败显示"未发现本地 LLM"占位项 + 刷新按钮）
- [2026-06-20] 工具栏改造（toolbar.py 右侧加 QComboBox + 刷新按钮，MainWindow 启动 QTimer.singleShot(100) 自动触发）
- [2026-06-20] 新增 refresh-active.svg 图标（Lucide refresh-cw 双圆弧箭头，generate_icons.py 注册 + 跑脚本生成）
- [2026-06-20] 测试补全（test_llm_ollama.py 11 例 + test_ui_toolbar.py 11 例，53 测试覆盖率 84.52%）
- [2026-06-20] PyInstaller 打 v0.0.4 .exe（47MB，零外部依赖，启动验证存活）
- [2026-06-20] ADR-018 追加（本地 LLM 检测首版选 Ollama + urllib 标准库 + QThread 异步检测 + 失败 UX 策略）
- [2026-06-20] UI 两项改进 v0.0.5（1）下拉框本地/云端分组：QComboBox 改用 QStandardItemModel，disabled 分组头标记 header role + 灰色粗体；本地组（Ollama 模型/占位）+ 云端组（3 家代表预置：gpt-4o/claude-opus-4-7/gemini-2.5-pro）；检测失败仅占位不加云端组（2）AI 气泡模型名标签：_add_message 加 model_name 参数，AI 行布局重构为 [垂直列：模型名 QLabel#ModelLabel + 气泡]；ChatPage.set_model_provider 注入回调，MainWindow 绑定 toolbar.current_model；resizeEvent 改用 _bubble_labels 列表批量更新
- [2026-06-20] 测试补全（test_ui_toolbar.py 13 例覆盖分组构造/header 不 emit/占位/云端切换/current_model 本地+云端/MainWindow 自动触发；test_ui_imports.py 补 3 例 AI 气泡含模型名/占位/用户气泡不带）
- [2026-06-20] PyInstaller 打 v0.0.5 .exe（47MB，零外部依赖，启动验证存活 pid=21684）
- [2026-06-20] ADR-019 追加（模型下拉框本地/云端分组 + AI 气泡模型名标签策略）
- [2026-06-20] v0.0.6 架构补齐：llm 层对称骨架（q_agent/llm/ollama.py 加 OllamaClient 子类 + 新建 q_agent/llm/cloud/{__init__,openai,anthropic,gemini}.py 三个 stub，所有 client 构造成功、chat/complete 抛 NotImplementedError；测试 15 例新增，总 74 通过，覆盖率 88.02%）
- [2026-06-20] 安装包规则改为只保留最新一代（CLAUDE.md §17 + 安装包/README.md + ADR-020 取代 ADR-010 的"历史版本长期保留"部分；v0.0.1~v0.0.5 .exe 子目录已删，仅留 v0.0.6）
- [2026-06-20] PyInstaller 打 v0.0.6 .exe（47MB，零外部依赖，启动验证存活 pid=21684/22444）
- [2026-06-20] ADR-020 追加（llm 层对称骨架 + 安装包只保留最新）
- [2026-06-20] v0.0.7 修复分类 bug：list_models() 返回 ModelEntry（name + is_remote + remote_host），通过 Ollama /api/tags 的 remote_model/remote_host 字段区分真正本地模型 vs Ollama Cloud 转发模型；UI 下拉框改三组（本地 + Ollama Cloud + 云端预置）；测试 7 例新增/重写，总 81 通过，覆盖率 88.15%
- [2026-06-20] PyInstaller 打 v0.0.7 .exe（47MB，零外部依赖，启动验证存活 pid=12440/16972）
- [2026-06-20] ADR-021 追加（Ollama Cloud 转发模型分类策略）- [2026-06-20] v0.0.8 通过 Ollama 唤醒模型 + 流式批量刷新：填充 OllamaClient.chat_stream（POST /api/chat stream=true NDJSON 解析）+ chat = join；新建 q_agent/ui/chat_worker.py ChatWorker QThread（500字/500ms 混合批量刷新阈值，避免一秒一字崩溃）；chat_page 接入真实调用（loading 状态 + pending AI 气泡 append_to_history=False + 三 slot chunk/failed/done）；toolbar 暴露 model_group_changed 信号 + current_model_group 方法供云端预置禁用发送按钮；theme 加 MessageError 红色气泡样式；_scroll_to_bottom 加 isValid 防御修测试跨用例残留崩；测试 12 例新建 + 6 例 chat_stream + 5 例 group + 6 例改写，总 106 通过，覆盖率 86.99%
- [2026-06-20] PyInstaller 打 v0.0.8 .exe（47MB，零外部依赖，--onefile --windowed）
- [2026-06-20] ADR-022 追加（通过 Ollama 唤醒模型 + 流式批量刷新 500字/500ms 混合阈值策略）
- [2026-06-20] v0.0.9 模型内存释放 + 切换模型清空上下文 + 模型名颜色：release_model POST /api/generate keep_alive=0 卸载模型出 Ollama RAM + toolbar release_btn + ModelReleaseWorker + 确认弹窗；切换模型清空对话 + 居中灰色斜体系统提示气泡"模型已切换为 XXX，上下文已清空"（首次自动选择抑制保留初始问候）；8 色调色板按模型名 zlib.crc32 hash 取色（同一模型每次同色），新增 release-active.svg 图标
- [2026-06-20] 测试补全（test_llm_ollama.py 4 例 release_model happy/http error/conn refused/timeout + test_ui_chat_page.py 10 例 _on_model_changed/_clear_messages/_model_color/_add_system_message/release_btn 三态/ModelReleaseWorker released/failed，总 120 通过，覆盖率 85.31%）
- [2026-06-20] PyInstaller 打 v0.0.9 .exe（47MB，零外部依赖，--onefile --windowed --clean，删除 v0.0.8 子目录仅留 v0.0.9）
- [2026-06-20] ADR-023 追加（模型内存释放 + 切换清空 + 模型名颜色策略）
- [2026-06-20] v0.0.10 加载指示器：新建 q_agent/ui/loading_dots.py LoadingDots(QWidget) 自定义 paintEvent（三点跳动 + HSV 流动彩虹 + alpha=180 半透明，QTimer 80ms tick 1.6s 周期）；chat_page _add_message 加 loading 参数 + _pending_loading 字段 + _remove_pending_loading 辅助；_on_chunk 首次 / _on_chat_failed / _on_chat_done / _clear_messages 均清理；测试 10 例新增（5 loading_dots 单测 + 5 chat_page 集成），总 130 通过，覆盖率 86.00%
- [2026-06-20] PyInstaller 打 v0.0.10 .exe（47MB，零外部依赖，--onefile --windowed --clean，删除 v0.0.9 子目录仅留 v0.0.10）
- [2026-06-20] ADR-024 追加（加载指示器四维度决策：三点跳动 + pending 气泡内 + 流动彩虹 + 元素本身半透明）
- [2026-06-20] v0.0.11 release_model 卸载验证：用户报告"内存释放失败 GPU 占用未变"，排查发现代码本身工作（curl + Python + nvidia-smi 均验证 VRAM 已归还），真正原因是 Ollama 进程级 CUDA context 不立即归还 OS（任务管理器看 ollama.exe 专用 GPU 内存列仍显示占用，但 /api/ps 已空 + nvidia-smi VRAM 已降）；新增 _is_model_loaded 辅助 + release_model 加 verify 轮询 /api/ps 3 次（间隔 300ms）确认模型从加载列表移除，未卸载时抛 OllamaError；toolbar 状态栏文案明确"API 验证通过，VRAM 已归还，任务管理器进程级 GPU 内存可能延迟显示"；测试 5 例新增/改写，总 134 通过，覆盖率 85.43%
- [2026-06-20] PyInstaller 打 v0.0.11 .exe（47MB，零外部依赖，--onefile --windowed --clean，删除 v0.0.10 子目录仅留 v0.0.11）
- [2026-06-20] ADR-025 追加（release_model 卸载验证决策 + 用户报告排查记录 + Ollama 进程级 CUDA context 不归还 OS 已知行为说明）
- [2026-06-20] v0.0.12 硬件监控曲线：sidebar 底部常驻 HardwareMonitor(QWidget 自绘 4 折线 60s 历史，CPU/GPU/VRAM/RAM 4 色区分，1s 采样)；引入 psutil + pynvml 运行时依赖（PyInstaller --collect-all 打包进 .exe，符合 ADR-015）；HardwareMonitorWorker QThread 后台采集，sample_collected 信号回主线程；无 NVIDIA 显卡时 GPU/VRAM 折线画灰色 N/A 占位（None 段断开不连线）；closeEvent 优雅停止 worker 避免线程悬挂；sidebar 从 QListWidget 改为 QFrame 容器（含 tab 列表 + HardwareMonitor）；测试 10 例新增 + 2 例改写（sidebar._list 适配），总 144 通过，覆盖率 84.24%
- [2026-06-20] PyInstaller 打 v0.0.12 .exe（47MB，零外部依赖，--onefile --windowed --clean --collect-all pynvml --collect-all psutil，删除 v0.0.11 子目录仅留 v0.0.12，启动验证版本号 0.0.12）
- [2026-06-20] ADR-026 追加（硬件监控曲线决策：sidebar 底部常驻 4 折线 + psutil + pynvml 运行时依赖 + 60s 历史 + None 段断开 + closeEvent 优雅退出）

- [2026-06-20] v0.0.13 治标不治本尝试（仅修 QSS 选择器改名 #SidebarList，未 commit，被 v0.0.14 覆盖）：用户验证后 tab 大小恢复但**图标仍未出现 + tooltip 消失**，根因是 v0.0.12 打包命令漏 `--add-data` 导致 SVG 没进 .exe（独立低级错误）+ sidebar 容器化结构本身违反贴纸式原则
- [2026-06-20] **v0.0.14 贴纸式重构治本回退**：用户提出"贴纸式开发"哲学批评（增加功能 = 在空白处贴贴纸，既有模块零改动），AI 三层反思后治本回退——① sidebar.py 恢复 v0.0.9 QListWidget 子类原貌（零内嵌 HardwareMonitor）② main_window.py 新建 left panel 一行 addWidget 贴纸式挂载 HardwareMonitor（sidebar 完全不碰）③ theme.py 4 处 QSS 选择器改回 `#Sidebar` ④ 打包命令严格按 README.md 模板（含 --add-data 修复 SVG 漏打包）⑤ CLAUDE.md 新增第二十一节"贴纸式开发原则" + 第十七节补充"打包命令必须严格复制粘贴" ⑥ ADR-027 贴纸式原则 + ADR-028 v0.0.12 回退决策。测试 144 通过，84.25% 覆盖率，ruff/mypy strict 全绿
- [2026-06-20] PyInstaller 打 v0.0.14 .exe（47MB，零外部依赖，严格按模板含 --add-data + --collect-all pynvml/psutil + --hidden-import pynvml，删除 v0.0.13 子目录仅留 v0.0.14，启动验证版本号 0.0.14）
- [2026-06-20] ADR-027 追加（贴纸式开发原则：基座永不动 + 新功能独立 widget + 父容器一行挂载 + 改既有类继承/objectName/公开接口 = 架构级改动触发第十九节先展示）
- [2026-06-20] ADR-028 追加（v0.0.12 sidebar 容器化违反贴纸式原则的回退决策，取代 ADR-026 中"Sidebar 从 QListWidget 改为 QFrame 容器"部分，ADR-026 其余 HardwareMonitor 指标/数据源/采样/渲染/容错决策保留不变）
