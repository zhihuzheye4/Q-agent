<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-19 23:00

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则
- 当前阶段：UI 界面空壳完成 + 第一个 .exe v0.0.1 已生成，等待用户提下一个需求

## 二、上次讨论

- 用户三步指令：(1) 修正 ADR-014 为方案 D (2) 用方案 D 执行 UI 设计 (3) 开发活的 UI 界面（界面可跳转、按钮可按，无实际功能）
- 我答：追加 ADR-016 取代 ADR-014 方案 C 部分 + 调 ui-ux-pro-max skill 产设计规范 + 实施 q_agent/ui/ 骨架 + 打 v0.0.1 .exe
- 用户确认 OK，继续

## 三、上次执行

- 追加 ADR-016（取代 ADR-014 方案 C 部分，承认 11:1 比分过激真实 6:2~7:3）
- 装 PySide6 6.11.1 + PyInstaller 6.21.0 到 F 盘 venv，加入 pyproject.toml dev 依赖
- 调 /ui-ux-pro-max --design-system 产设计规范，存 memory/设计规范/MASTER.md
  - Style: Dark Mode (OLED) / 10 个语义 color token / Inter 字体 / WCAG AAA
  - 翻译为 QSS 全局主题（DARK_QSS，含侧边栏/按钮/输入框/工具栏/菜单/设置/滚动条/焦点环等组件样式）
- 写方案文件 memory/方案/方案_2026-06-19_2010_UI界面开发.md
- 追加 6 个 UI 图标定义到 scripts/generate_icons.py：chat / skills / memory / new-chat / clear / about
- 跑脚本生成 11 个 SVG + manifest.json，全部 XML well-formed
- 实施 q_agent/ui/ 模块：
  - icons.py（方案 D 核心：QIcon 直接受 SVG + manifest 索引 + importlib.resources 兼容 PyInstaller）
  - theme.py（QSS 暗色主题 + 10 个 color token 常量）
  - main_window.py（QMainWindow + 侧边栏 + QStackedWidget + 工具栏 + 菜单栏 + 状态栏）
  - sidebar.py（QListWidget 4 tab + 图标 + 切换信号）
  - toolbar.py（QToolBar 3 图标按钮：新建对话/清空/关于）
  - menu_bar.py（文件→退出 + 帮助→关于弹窗）
  - pages/chat_page.py（消息流 + 输入框 + 发送按钮 + Enter 发送 + echo 回声占位）
  - pages/skills_page.py（占位列表 4 项）
  - pages/memory_page.py（占位列表 5 项）
  - pages/settings_page.py（通用/LLM/工具调用层 3 组控件：开关+下拉+数值）
- cli.py 新增 ui 子命令（延迟 import PySide6）+ 修复缺 `if __name__ == "__main__":` 守护（关键 bug！）
- 写 tests/test_ui_imports.py：11 测试（icons/theme/main_window/sidebar/chat/settings/cli 全覆盖）
- 工具链全绿：ruff check + ruff format --check + mypy strict + pytest -q（31 测试通过，覆盖率 84.82%）
- UI 启动测试通过（offscreen 模式 4 tab 切换 + 输入框启用 + echo 消息追加）
- PyInstaller 打 v0.0.1 .exe：
  - 关键发现：cli.py 缺 `if __name__ == "__main__":` 守护时，PyInstaller --onefile 打的 .exe 启动后只 import 模块不调 main()！症状是 .exe 跑 version 命令无 stdout 输出，加守护后正常输出 "Q-agent 0.0.1"
  - 命令：pyinstaller --onefile --windowed --add-data "q_agent/assets;q_agent/assets" q_agent/cli.py
  - 产物：安装包/v0.0.1/Q-agent.exe（47MB，含 PySide6 + Qt 全套）
  - 验证：Start-Process 启动 UI 进程持续运行 PID 14004，零外部依赖
- 更新 安装包/README.md 版本历史表追加 v0.0.1

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ commit cba9d0d（框架 + 审查修复）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度）
- ✅ commit 862a6ea（新规则 + 新文件夹）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本开发（5 级递进测试通过 + 参考 Lucide 风格优化 + 测试产物已清理）
- ✅ commit 3d44456（矢量图脚本 + 第二十节 + ADR-013/014/015 + 思维导图 + 记忆同步）
- ✅ ADR-016 追加（取代 ADR-014 方案 C，改方案 D）
- ✅ PySide6 + PyInstaller 安装到 F 盘 venv
- ✅ 设计系统规范产出（memory/设计规范/MASTER.md）
- ✅ UI 图标生成（11 SVG + manifest.json，6 个 UI 用 + 5 个测试样本）
- ✅ q_agent/ui/ 模块实施（方案 D：icons/theme/main_window/sidebar/toolbar/menu_bar/pages）
- ✅ 测试 + 工具链全绿（31 测试 + 84.82% 覆盖率 + ruff + mypy strict）
- ✅ PyInstaller 打 v0.0.1 .exe（47MB，零外部依赖，UI 能启动）
- ⏳ 待 commit 本次工作
- ⏳ 待 git push（4 次 commit 待推：cba9d0d + 862a6ea + 3d44456 + 本次）

## 五、下一步

- [ ] commit 本次工作（ui 模块 + 图标 + 设计规范 + .exe + ADR-016 + 第二十节已在前次 + 方案归档 + 记忆同步）
- [ ] git push（4 次 commit 待推）
- [ ] 用户提下一个需求（候选：接入本地 LLM / 设置持久化 / 快捷键 / 多窗口等）

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（PyInstaller + 安装包/v{版本}/）
- ADR-011：UI 矢量图资源隔离（q_agent/assets/icons/）
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D（取代 ADR-014 方案 C 部分）

## 七、未解决问题

- github.com 历史不可达问题可能复发，push 失败时记录到全局错误档案
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed 模式下 GUI 应用 console handle 继承问题）——不影响用户体验（双击 .exe 是 GUI 启动）
- 软件实际功能仍仅 echo 一条 + UI 空壳可跳转，无 LLM/真实工具/持久记忆
- 设计规范 spacing/shadow 表是推测值（未调 skill --persist 补全），后续可升级