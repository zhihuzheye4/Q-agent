<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-20 12:00

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则、**安装包只保留最新一代**
- 当前阶段：v0.0.6 完成（llm 层对称骨架：OllamaClient 子类 + cloud/{openai,anthropic,gemini}.py stubs；安装包规则改为只留最新），等待用户提下一个需求

## 二、上次讨论

用户三问（v0.0.6 决策来源）：
1. 软件包不需要保留每一代，只需要保留最新一代即可
2. 当前软件并没有区分云端和本地，或你编写的 py 文件并没有区分云端和本地的能力？（质疑后端结构）
3. 是否严格遵循模块化？为什么不先完成最小闭环再添加枝叶？（解释：现在解决掉后期尽量不动，避免代码改动带来的 bug）

决策确认（AskUserQuestion）：
- 补齐 llm 对称骨架（推荐）——v0.0.6 落地
- 安装包改为只保留最新——v0.0.6 落地

## 三、上次执行

- 新建 `q_agent/llm/cloud/` 子包：`__init__.py` + `openai.py` + `anthropic.py` + `gemini.py`
  - 三个 stub：构造 (model, api_key, base_url) + chat/complete 抛 NotImplementedError
  - 依赖坚守 ADR-015：urllib 标准库，不引入 SDK
- 升级 `q_agent/llm/ollama.py`：新增 `OllamaClient(LLMClient)` 子类，构造 (model, host) + chat/complete 抛 NotImplementedError。list_models() 函数保留
- 修改 `q_agent/llm/__init__.py`：统一导出 LLMClient / OllamaClient / OllamaError / list_models / LocalLLMClient / OpenAIClient / AnthropicClient / GeminiClient
- 新增 `tests/test_llm_clients.py`：15 例覆盖抽象基类/构造/NotImplementedError/子类关系/模块导出
- 修复 4 处 E501 行太长（docstring + NotImplementedError 消息）
- 工具链全绿：ruff check + ruff format + mypy strict + pytest -q（74 测试，覆盖率 88.02%）
- CLAUDE.md §17 改为"只保留最新一代"
- 安装包/README.md 重写：规则改为只留最新，新增版本历史表保留 v0.0.1~v0.0.6 演进记录
- 删除 v0.0.1~v0.0.5 .exe 子目录，仅留 v0.0.6
- v0.0.6 .exe 打包：47MB 零外部依赖，PowerShell Start-Process 验证存活 pid=21684/22444
- __version__ 0.0.5 → 0.0.6
- ADR-020 追加（llm 层对称骨架 + 安装包只保留最新，取代 ADR-010 的"历史版本长期保留"部分）
- 思维导图数据.jsonl 追加一行 + 跑 update_mindmap.py
- 技能工具注册清单追加 LLM 层对称骨架条目
- 项目指导文件更新当前节点 + 已完成节点
- 编年史记忆已备份旧临时记忆完整快照
- 方案归档：方案_2026-06-20_xxxx_llm层对称骨架与安装包只留最新.md → memory/方案/已执行/
- commit 5cff4c3：补齐 llm 层对称骨架 + 安装包只保留最新
- commit fb45de8：善后同步 v0.0.6 版本号 + 安装包 README 版本表

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本（5 级递进测试通过 + Lucide 风格优化 + refresh 图标加入）
- ✅ ADR-016 方案 D（QIcon 直接受 SVG）
- ✅ 设计系统规范产出（memory/设计规范/MASTER.md）
- ✅ UI 图标生成（12 SVG + manifest.json，含 refresh-active）
- ✅ q_agent/ui/ 模块实施（方案 D）
- ✅ PyInstaller 打 v0.0.1~v0.0.6 .exe（按新规则只留 v0.0.6）
- ✅ UI 改进两轮（tooltip 透明 + 气泡宽度 0.92 + Feather 齿轮 + 多行输入框 + 输入框动态高度）
- ✅ 本地 Ollama 模型检测（q_agent/llm/ollama.py list_models + toolbar 模型下拉框 + ModelRefreshWorker QThread 异步）
- ✅ 下拉框本地/云端分组 + AI 气泡模型名标签（v0.0.5）
- ✅ llm 层对称骨架（OllamaClient 子类 + cloud stubs）（v0.0.6）
- ✅ 安装包规则改为只保留最新（v0.0.6）
- ✅ ADR-020 追加

## 五、下一步

- [ ] git push（GitHub 认证之前错误 056 已自愈，可能仍可推）
- [ ] 用户提下一个需求（候选：模型选中后接 chat 对话 / 设置持久化 QSettings / 快捷键 / 多窗口 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接云端 API / 填 chat/complete 方法体）

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（PyInstaller + 安装包/v{版本}/；"历史版本长期保留"部分已被 ADR-020 取代）
- ADR-011：UI 矢量图资源隔离（q_agent/assets/icons/）
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D（取代 ADR-014 方案 C 部分）
- ADR-017：UI 输入框动态高度策略
- ADR-018：本地 LLM 检测首版选 Ollama + urllib 标准库 + QThread 异步
- ADR-019：模型下拉框本地/云端分组 + AI 气泡模型名标签
- ADR-020：llm 层对称骨架 + 安装包只保留最新（取代 ADR-010 "历史版本长期保留"部分）

## 七、未解决问题

- github.com 历史不可达问题可能复发，push 失败时记录到全局错误档案
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed 模式 GUI 应用 console handle 继承问题）——不影响用户体验
- 软件功能仍仅 echo + UI 跳转 + 模型下拉框检测 + 分组 + AI 气泡模型名 + llm 对称骨架（构造 OK，chat/complete 仍 NotImplementedError）
- 选了模型后无真实对话（chat() 未实现）——下一候选需求
- 设计规范 spacing/shadow 表是推测值（未调 skill --persist 补全）
- 仅支持 Ollama 本地 + 3 家云端预置占位，未接任何云端 API（cloud stubs 仅骨架）
- 模型选择未持久化（QSettings 落盘是下一候选需求）