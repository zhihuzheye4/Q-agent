<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-19 19:57

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则
- 当前阶段：项目框架 + 审查修复 + 新规则制定 + 矢量图生成脚本开发均完成，工具链全绿，等待用户提下一个需求

## 二、上次讨论

- 用户通过 plan，提两个关键问题：依赖哲学（1G 软件不依赖 20G 外部库）+ 位图思维（SVG→PNG→UI 调 PNG 过时）
- 我答：开发期依赖 ≠ 运行时分发依赖；推荐方案 C（SVG sprite + QtSvg 首次缓存）放弃位图思维
- 用户升级硬规则：禁止让用户装任何外部依赖（"买手机不能要求先买光刻机"），写进 CLAUDE.md 第二十节
- 用户两条最后提醒：PNG 转换按需使用（像电脑开机不需要把所有软件全开）+ 测试产物用完即删
- 用户要求优化线条细节，参考互联网公开矢量图网站质量（参考 Lucide / Feather 风格）
- 用户确认 OK，继续

## 三、上次执行

- 写硬规则：CLAUDE.md 第二十节"分发零外部依赖"+ ADR-013/014/015 + 思维导图 2 行
- 开发 scripts/generate_icons.py：
  - 5 级递进复杂度图标（L1 circle / L2 send / L3 settings / L4 cloud-sync / L5 ai-brain）
  - primitive（circle/rect/path/polygon/line/group）+ 渐变（linear/radial）+ 滤镜（drop_shadow）
  - 渲染器（顶层 linecap/linejoin round + stroke-width 2）
  - 输出器（write_svg + svg_to_png 延迟 import PySide6 + write_manifest + clean_output）
  - argparse：--png / --output / --clean
- 工具链全绿：ruff check + ruff format + mypy strict
- 跑脚本生成 5 个测试 SVG，XML 全部 well-formed
- 修复 bug：SVG 标准属性名（stop-color/flood-color/flood-opacity/stroke-width）误用下划线
- 优化线条细节：参考 Lucide / Feather 风格
  - 顶层加 stroke-linecap="round" + stroke-linejoin="round" + stroke-width=2
  - L1 circle: r=9 视觉饱满
  - L2 send: Lucide send 官方 path
  - L3 settings: Lucide settings 官方 path（复杂 path 描 8 齿轮廓 + 中心圆）
  - L4 cloud-sync: Lucide cloud 官方 path + 渐变 + refresh 风格同步箭头
  - L5 ai-brain: 大脑 path 对称优化 + 保留多色渐变/滤镜/电路
- 5 级全部稳定输出：文件 0.20~2.13 KB，生成时间 < 1ms，L5 接近上限但仍可读
- 用 --clean 删除 6 个测试产物（5 SVG + manifest.json），icons/ 只剩 .gitkeep
- 更新 assets/README.md（脚本用法 + 调用方案 + 硬规则）+ 技能工具注册清单 + 方案归档 + 项目指导文件

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ commit cba9d0d（框架 + 审查修复）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度）
- ✅ commit 862a6ea（新规则 + 新文件夹）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本开发（5 级递进测试通过 + 参考 Lucide 风格优化 + 测试产物已清理）
- ⏳ 待 commit 本次工作
- ⏳ 待 PyInstaller + PySide6 安装 + 打 v0.0.1 第一个 .exe
- ⏳ 待 UI 框架实施

## 五、下一步

- [ ] commit 本次工作（脚本 + 第二十节 + ADR-013/014/015 + 思维导图 + 记忆同步）
- [ ] git push（多次 commit 待推：cba9d0d + 862a6ea + 本次）
- [ ] 装 PySide6 + PyInstaller 到 F 盘 venv
- [ ] 实施 q_agent/ui/ 主窗口骨架（调 /ui-ux-pro-max skill 产设计系统规范）
- [ ] UI 加载 manifest.json 调用预制 SVG 图标（方案 C：SVG sprite + QtSvg 首次缓存）
- [ ] 打 v0.0.1 .exe（含 UI 空壳）到 安装包/v0.0.1/

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（PyInstaller + 安装包/v{版本}/）
- ADR-011：UI 矢量图资源隔离（q_agent/assets/icons/）
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（方案 C：SVG sprite + QtSvg 首次缓存）
- ADR-015：分发零外部依赖硬规则

## 七、未解决问题

- github.com 历史不可达问题可能复发，push 失败时记录到全局错误档案
- PyInstaller + PySide6 未装，v0.0.1 第一个 .exe 待用户确认后生成
- 软件实际功能仍仅 echo 一条，无 LLM/真实工具/UI/持久记忆