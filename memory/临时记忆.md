<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-20 13:40

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则、安装包只保留最新一代
- 当前阶段：v0.0.10 完成（加载指示器：pending AI 气泡内三点跳动 + 流动彩虹 + 半透明），等待用户提下一个需求

## 二、上次讨论

用户提一个新功能需求：
> 添加一个进度条，或正在加载显示，彩色+高透明度

背景：v0.0.8 接通 Ollama 流式后，用户发送对话到首个 chunk 到达之间有数百毫秒~数秒"空等期"，期间 UI 只显示"生成中"按钮文字，pending AI 气泡为空 QLabel（高度 0 看不见），用户看不出 AI 是否在跑。

用户对关键 UX 决策的回答（AskUserQuestion 答复）：
- 形式：**三点跳动**（iMessage 打字气泡风格，轻量友好）
- 位置：**pending AI 气泡内**（贴近用户视线）
- 彩色：**流动彩虹渐变**（HSV 色相循环，独立于模型色，活泼）
- 透明度：**加载指示元素本身半透明**（alpha=180 ≈ 70%，不刺眼）

## 三、上次执行

- 新建 `q_agent/ui/loading_dots.py`：`LoadingDots(QWidget)` 自定义 paintEvent
  - 三个圆点（DOT_SIZE=8, DOT_GAP=8）依次上下跳动（DOT_BOUNCE=5px）
  - 相位偏移 `i * 0.33` 让三个点错开 1/3 周期 → 依次跳动
  - 跳动用 `-abs(math.sin(t * 2π))` 平滑上跳下落
  - 颜色 `QColor.fromHsv((phase*360 + i*60) % 360, 220, 230, 180)` → 流动彩虹
  - QTimer 80ms tick，phase += 0.05，1.6s 一周期
  - 父 widget 销毁时 QTimer 自动停止（Qt parent 机制）
- `q_agent/ui/pages/chat_page.py` 集成：
  - 新字段 `_pending_loading: LoadingDots | None`
  - `_add_message` 加 `loading: bool = False` 参数，pending AI 气泡用 True → 在模型名与气泡之间插入 LoadingDots
  - 新增 `_remove_pending_loading()` 辅助方法（hide + deleteLater + 清引用）
  - `_on_chunk` 首次调用（_pending_loading 非 None）→ 移除 LoadingDots（回复开始流入）
  - `_on_chat_failed` / `_on_chat_done` / `_clear_messages` 均清理 LoadingDots
  - import `from q_agent.ui.loading_dots import LoadingDots`
- 测试：
  - 新建 `tests/test_ui_loading_dots.py` 5 例（构造固定尺寸 / QTimer tick 推进 phase / paintEvent 不崩 / 多 phase 渲染不崩 / parent 销毁跟随）
  - `tests/test_ui_chat_page.py` 追加 5 例集成测试（_on_send_clicked 后 _pending_loading 非 None / 首次 chunk 移除 / _on_chat_failed 移除 / _on_chat_done 空回复移除 / _clear_messages 清引用）
- 工具链全绿：ruff format + ruff check + mypy strict + pytest -q（130 测试，覆盖率 86.00%）
- __version__ 0.0.9 → 0.0.10
- v0.0.10 .exe 打包：47MB 零外部依赖，PyInstaller --onefile --windowed --clean
- 删除 v0.0.9 子目录，仅留 v0.0.10（按"只留最新"规则）
- 安装包/README.md 版本历史表追加 v0.0.10 行
- ADR-024 追加（加载指示器四维度决策 + 实施细节 + 取舍）
- 待办：commit + push / 思维导图数据追加 / 技能注册清单追加 / 项目指导文件更新 / 方案归档

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本 + ADR-016 方案 D + 设计系统规范 + UI 图标 + q_agent/ui/ 模块
- ✅ PyInstaller 打 v0.0.1~v0.0.10 .exe（按新规则只留 v0.0.10）
- ✅ UI 改进两轮 + 输入框动态高度
- ✅ 本地 Ollama 模型检测 + 下拉框本地/云端分组 + AI 气泡模型名标签
- ✅ llm 层对称骨架（OllamaClient 子类 + cloud stubs）
- ✅ 安装包规则改为只保留最新
- ✅ Ollama Cloud 转发模型分类修复（三组结构 + ModelEntry）
- ✅ 通过 Ollama 真实唤醒模型 + 流式批量刷新（ChatWorker + 500字/500ms 混合阈值）
- ✅ 内存释放 + 切换模型清空上下文 + 模型名 hash 颜色
- ✅ 加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ✅ ADR-024 追加

## 五、下一步

- [ ] git commit v0.0.10 代码 + 记忆同步（本地领先 origin/main 多个 commit 待推）
- [ ] git push（GitHub 认证失败问题仍未解决，需用户介入）
- [ ] 用户提下一个需求（候选：取消按钮接 ChatWorker.stop() / 设置持久化 QSettings / 多轮上下文裁剪 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接 OpenAI/Anthropic/Google API / 历史消息窗口策略 / 释放成功后下拉框不选中此模型 / 新建对话按钮实际行为 / 清空按钮实际行为 / 加载时长统计 + 慢响应警告）

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（"历史版本长期保留"部分已被 ADR-020 取代）
- ADR-011：UI 矢量图资源隔离
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D
- ADR-017：UI 输入框动态高度策略
- ADR-018：本地 LLM 检测首版选 Ollama + urllib + QThread 异步
- ADR-019：模型下拉框本地/云端分组 + AI 气泡模型名标签
- ADR-020：llm 层对称骨架 + 安装包只保留最新
- ADR-021：Ollama Cloud 转发模型分类（remote_model / remote_host 字段判定）
- ADR-022：通过 Ollama 唤醒模型 + 流式批量刷新（500字/500ms 混合阈值）
- ADR-023：模型内存释放 + 切换清空上下文 + 模型名颜色
- ADR-024：加载指示器（三点跳动 + 流动彩虹 + 半透明）

## 七、未解决问题

- github.com 历史不可达问题第三次复发，push 失败需用户处理（控制面板凭据管理器删除 github.com 旧凭据 → 下次 push 触发重新认证 / git credential-manager configure / SSH key 替代 HTTPS）
- 本地领先 origin/main 共 6 个 commit 待推（408787a/10ae75e/38dc057/8be1eff + v0.0.10 新 commit）
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed GUI 应用 console handle 继承）——不影响用户体验
- 取消按钮未接 ChatWorker.stop()（接口已留，v0.0.11+ 接）
- 历史消息首版全传（token 消耗大但简单），上下文裁剪留 v0.0.11+
- host 仍 hardcoded "http://localhost:11434"，QSettings 持久化留 v0.0.11+
- 设计规范 spacing/shadow 表是推测值
- CLOUD_PRESET 仍是硬编码占位，未接 OpenAI/Anthropic/Google API（cloud stubs 仅骨架）
- 模型选择未持久化（QSettings 落盘是下一候选需求）
- Ollama Cloud 判定依赖 remote_model/remote_host 字段，老版 Ollama 可能不返回，退化为 v0.0.6 行为（graceful，非问题）
- release_model 调用时如果模型正在 chat 生成中，Ollama 在当前生成完成后立即卸载（keep_alive=0 不强制中断当前生成）
- 切换模型清空对话是硬规则，不保留历史；用户切回原模型需重新输入
- 模型名 hash 颜色可能碰撞（crc32 mod 8 同值），8 色调色板日常足够，碰撞概率 1/8 可接受
- 新建对话/清空按钮仍是活 UI 空壳，未接 chat_page._clear_messages
- LoadingDots 固定 dot_size=8 不随 DPI 缩放（高 DPI 屏可能偏小）
- LoadingDots 流动彩虹色相独立于模型色，与"模型名 hash 色"色彩系统并行（设计取舍，非冲突）