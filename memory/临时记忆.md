<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-20 17:30

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则、安装包只保留最新一代、**贴纸式开发原则（v0.0.14 新增，CLAUDE.md 第二十一节）**、**打包命令严格从 README.md 复制粘贴不得手敲（v0.0.14 新增，CLAUDE.md 第十七节补充）**
- 当前阶段：v0.0.14 完成（贴纸式重构治本回退 v0.0.12 sidebar 容器化错误），等待用户验证 .exe 双击效果

## 二、上次讨论

用户在 v0.0.13 验证后严肃批评："增加功能按键就好像在空白的地方贴一张贴纸，能点击这个按钮影响到的代码也只是这个功能相关的代码。当前这个状态和我一开始设计的严重背离。"

用户进一步阐述设计哲学：UI 所有图片存一个文件夹，软件启动先唤醒 UI 窗口，UI 元素用坐标定位（如设置图标在 10,20），代码引用方式"当 X 图标被点击 → 执行这些程序"，新增/删除按钮只动该按钮代码不影响其他，按钮之间互不依赖，一个崩溃不引起整个窗口错误。

用户问这种思路叫什么、有没有更好更成熟的。AI 回答：用户思路叫"绝对坐标布局/画布式 GUI"（VB/Delphi/Win32 风格），现代等价物是"Qt 布局管理器 + 独立 widget 模块"——满足同样隔离需求（按钮独立/增删零连锁/资源集中/回调简单）+ 自动响应式 + 动态内容原生支持。资源占用三者几乎相同（差异 < 1KB，被 PySide6 100MB 级开销淹没），选择不该基于资源占用。

用户确认按"Qt 布局管理器 + 独立 widget + 严格贴纸式"重构 v0.0.14。

## 三、上次执行（v0.0.14 贴纸式重构治本回退）

### 架构回退（治本）

- **sidebar.py 恢复 v0.0.9 QListWidget 子类原貌**：
  - `class Sidebar(QListWidget)`，objectName="Sidebar"
  - 4 tab（对话/技能/记忆/设置）+ 图标 + tooltip + tab_changed 信号
  - **零内嵌 HardwareMonitor**（HardwareMonitor 不再是 sidebar 子组件）
  - 保留 SIDEBAR_WIDTH=200 兼容历史

- **main_window.py 新建 left panel 贴纸式挂载**：
  - left panel = QWidget + QVBoxLayout（sidebar stretch=1 + hardware_monitor 底部固定 160px）
  - left_panel.setFixedWidth(LEFT_PANEL_WIDTH=200) 放进水平布局左侧
  - `self.sidebar = Sidebar(left_panel)` + `self.hardware_monitor = HardwareMonitor(left_panel)` 两行 addWidget
  - closeEvent 调 `self.hardware_monitor.stop()` 优雅退出
  - QTimer.singleShot(200, self.hardware_monitor.start) 启动 worker
  - 新增 import：HardwareMonitor + QVBoxLayout + SIDEBAR_WIDTH

- **theme.py 恢复 QSS 选择器**：
  - 4 处 `QListWidget#SidebarList` 改回 `QListWidget#Sidebar`（v0.0.9 原貌）
  - 删除 v0.0.13 新增的 `QFrame#Sidebar` 和 `QListWidget#SidebarList` 选择器

- **hardware_monitor.py 保持不变**（已是独立模块，符合贴纸式）

### 测试改造

- `tests/test_ui_imports.py`：
  - `test_main_window_constructs`：`w.sidebar._list.count()` → `w.sidebar.count()`（sidebar 恢复 QListWidget 子类，count() 直接可用）；`w.sidebar.hardware_monitor is not None` → `w.hardware_monitor is not None`（hardware_monitor 改由 MainWindow 直接持有）
  - `test_sidebar_tab_switches_stack`：`w.sidebar._list.setCurrentRow(i)` → `w.sidebar.setCurrentRow(i)`

### 工具链

- ruff format 修了 main_window.py + sidebar.py 格式（内容不变）
- ruff check + mypy strict + pytest 全绿
- pytest：144 通过，覆盖率 84.25%

### 版本升级 + 打包

- __version__ 0.0.13 → 0.0.14
- PyInstaller 严格按 README.md 标准模板：`--onefile --windowed --clean --noconfirm --add-data "q_agent/assets;q_agent/assets" --hidden-import pynvml --collect-all pynvml --collect-all psutil q_agent/cli.py`
- **修复 v0.0.12 漏 `--add-data` 的低级错误**（SVG 图标资源现在正确打包进 .exe）
- 47MB 零外部依赖
- 删除 v0.0.13 子目录，仅留 v0.0.14
- 启动验证：`Q-agent.exe version` → `Q-agent 0.0.14`

### 规则固化（防止再犯）

- **CLAUDE.md 新增第二十一节"贴纸式开发原则"**：
  - 基座（v0.0.1 写好的 UI 框架）永不再动（除非明确扩展公开接口）
  - 贴纸 = 新功能独立 widget 文件 + 父容器一行 addWidget 挂载 + 信号槽连接
  - 架构级改动判定：改既有类继承/objectName/公开接口/塞新功能进既有模块内部 → 触发第十九节"先展示方案让用户否决"
  - 违规自检：每次写代码前自问"这个改动是否动了既有模块的类继承/objectName/公开接口/内部结构？"——若是，停止，先展示方案

- **CLAUDE.md 第十七节补充"打包命令固化"**：
  - 打包命令必须严格从 `安装包/README.md` 标准模板复制粘贴，不得手敲删参数
  - 历史教训：v0.0.12 手敲漏 `--add-data`，SVG 没进 .exe，tab 图标消失
  - 如需新增/修改参数，先更新 README.md 模板再复制执行

- **ADR-027**：贴纸式开发原则（基座+扩展槽+独立 widget+信号槽解耦+架构级改动判定）
- **ADR-028**：v0.0.12 sidebar 容器化违反贴纸式原则的回退决策（取代 ADR-026 中"Sidebar 从 QListWidget 改为 QFrame 容器"部分，ADR-026 其余 HardwareMonitor 决策保留不变）

### 记忆同步

- ADR-027 + ADR-028 追加到 `memory/ADR架构决策记录.md`
- 思维导图数据.jsonl 追加 v0.0.14 一行 → update_mindmap.py 重新渲染（22 存活 + 0 删除）
- 技能工具注册清单追加 v0.0.14 条目（Sidebar 恢复 QListWidget + MainWindow left panel + theme QSS 恢复 + 打包命令固化）
- 项目指导文件更新当前节点（v0.0.14 完成）+ 追加已完成节点
- 安装包/README.md 打包命令模板补全（含 --add-data + --hidden-import + --collect-all）+ v0.0.14 版本历史行 + v0.0.13 行标注"未 commit 被覆盖"
- 临时记忆备份编年史 + 重写为 v0.0.14 完成版

### 待办

- 方案归档到 `memory/方案/已执行/`
- git commit v0.0.14 代码 + 规则 + 记忆同步
- git push
- **等用户双击 v0.0.14 .exe 验证**：tab 大小/图标/tooltip 是否恢复正常，硬件监控曲线是否正常显示

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度 + **贴纸式开发原则 v0.0.14**）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本 + ADR-016 方案 D + 设计系统规范 + UI 图标 + q_agent/ui/ 模块
- ✅ PyInstaller 打 v0.0.1~v0.0.14 .exe（按新规则只留 v0.0.14）
- ✅ UI 改进两轮 + 输入框动态高度
- ✅ 本地 Ollama 模型检测 + 下拉框本地/云端分组 + AI 气泡模型名标签
- ✅ llm 层对称骨架（OllamaClient 子类 + cloud stubs）
- ✅ 安装包规则改为只保留最新
- ✅ Ollama Cloud 转发模型分类修复（三组结构 + ModelEntry）
- ✅ 通过 Ollama 真实唤醒模型 + 流式批量刷新（ChatWorker + 500字/500ms 混合阈值）
- ✅ 内存释放 + 切换模型清空上下文 + 模型名 hash 颜色
- ✅ 加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ✅ release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）
- ✅ 硬件监控曲线（HardwareMonitor 独立 widget + MainWindow left panel 贴纸式挂载 + psutil + pynvml）
- ✅ ADR-026 追加（v0.0.12 硬件监控曲线决策，部分被 ADR-028 取代）
- ✅ ADR-027 追加（贴纸式开发原则）
- ✅ ADR-028 追加（v0.0.12 sidebar 容器化回退决策）
- ✅ CLAUDE.md 第二十一节贴纸式开发原则 + 第十七节打包命令固化

## 五、下一步

- [ ] 方案归档到 `memory/方案/已执行/`
- [ ] git commit v0.0.14 代码 + 规则 + 记忆同步
- [ ] git push
- [ ] **用户双击 v0.0.14 .exe 验证**：tab 大小/图标/tooltip/硬件监控曲线
- [ ] 用户提下一个需求（候选：取消按钮接 ChatWorker.stop() / 设置持久化 QSettings / 多轮上下文裁剪 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接 OpenAI/Anthropic/Google API / 历史消息窗口策略 / 释放成功后下拉框不选中此模型 / 新建对话按钮实际行为 / 清空按钮实际行为 / 加载时长统计 + 慢响应警告 / 硬件监控采样间隔可配置 / 硬件监控历史窗口可配置 / 硬件监控报警阈值）——**所有新功能必须遵循贴纸式开发原则（CLAUDE.md 第二十一节）：独立 widget + 父容器一行挂载 + 既有模块零改动**

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（"历史版本长期保留"部分已被 ADR-020 取代）
- ADR-011：UI 矢量图资源隔离
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D
- ADR-017：UI 输入框动态高度策略
- ADR-018：本地 LLM 检测首版选 Ollama + urllib + QThread 异步
- ADR-019：模型下拉框本地/云端分组 + AI 气泡模型名标签
- ADR-020：llm 层对称骨架 + 安装包只保留最新
- ADR-021：Ollama Cloud 转发模型分类（remote_model / remote_host 字段判定）
- ADR-022：通过 Ollama 唤醒模型 + 流式批量刷新（500字/500ms 混合阈值）
- ADR-023：模型内存释放 + 切换清空上下文 + 模型名颜色
- ADR-024：加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ADR-025：release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）
- ADR-026：硬件监控曲线（sidebar 底部常驻 4 折线 + psutil + pynvml + 60s 历史 + None 段断开）——"Sidebar 容器化"部分被 ADR-028 取代
- ADR-027：贴纸式开发原则（基座永不动 + 新功能独立 widget + 父容器一行挂载 + 架构级改动判定）
- ADR-028：v0.0.12 sidebar 容器化违反贴纸式原则的回退决策（取代 ADR-026 中"Sidebar 容器化"部分）

## 七、未解决问题

- github.com push 网络问题（v0.0.11/v0.0.12 push 成功了，但可能再触发；v0.0.14 待 push）
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed GUI 应用 console handle 继承）——不影响用户体验
- 取消按钮未接 ChatWorker.stop()（接口已留，v0.0.15+ 接）
- 历史消息首版全传（token 消耗大但简单），上下文裁剪留 v0.0.15+
- host 仍 hardcoded "http://localhost:11434"，QSettings 持久化留 v0.0.15+
- 设计规范 spacing/shadow 表是推测值
- CLOUD_PRESET 仍是硬编码占位，未接 OpenAI/Anthropic/Google API（cloud stubs 仅骨架）
- 模型选择未持久化（QSettings 落盘是下一候选需求）
- Ollama Cloud 判定依赖 remote_model/remote_host 字段，老版 Ollama 可能不返回，退化为 v0.0.6 行为（graceful，非问题）
- release_model 调用时如果模型正在 chat 生成中，Ollama 在当前生成完成后立即卸载（keep_alive=0 不强制中断当前生成）；验证步骤会检测到模型仍在 → 抛错提示"可能正在生成中，请稍后重试"
- 切换模型清空对话是硬规则，不保留历史；用户切回原模型需重新输入
- 模型名 hash 颜色可能碰撞（crc32 mod 8 同值），8 色调色板日常足够，碰撞概率 1/8 可接受
- 新建对话/清空按钮仍是活 UI 空壳，未接 chat_page._clear_messages
- LoadingDots 固定 dot_size=8 不随 DPI 缩放（高 DPI 屏可能偏小）
- LoadingDots 流动彩虹色相独立于模型色，与"模型名 hash 色"色彩系统并行（设计取舍，非冲突）
- **Ollama 进程级 CUDA context 不立即归还 OS**：release_model /api/ps 验证通过 + nvidia-smi VRAM 已归还，但任务管理器看 ollama.exe 进程的"专用 GPU 内存"列可能仍显示几 GB 占用，这是 Ollama 已知行为非项目 bug，状态栏文案已明确告知用户
- **硬件监控无 NVIDIA 显卡时 GPU/VRAM 永远 N/A**：pynvml 容错降级，AMD/Intel GPU 不支持（需引入额外依赖，等用户提需求）
- **硬件监控采样间隔固定 1s 不可配置**：未来可在设置中调 1s/2s/5s
- **硬件监控历史窗口固定 60s 不可配置**：未来可调 60s/120s/300s
- **硬件监控不写盘持久化**：关闭重启后历史清空
- **v0.0.12/v0.0.13 的教训已通过 ADR-027/028 + CLAUDE.md 第二十一节明文化**：贴纸式开发原则 + 打包命令固化，下次 AI 在规则层面被拦住，不靠判断力