<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-20 21:50

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则、安装包只保留最新一代、**贴纸式开发原则（v0.0.14 第二十一节）**、**打包命令严格从 README.md 复制粘贴不得手敲（v0.0.14 第十七节补充）**
- 当前阶段：v0.0.15 完成（硬件监控独立窗口 + menu_bar 回调模式），等待用户验证 .exe

## 二、上次讨论

用户在 v0.0.14 验证"正常"后给出 v0.0.15 需求："继续，把当前的 GPU 监控相关的从原来的地方移除，在上方文件/帮助的菜单栏添加监控按钮，点击监控会弹出一个独立窗口，显示当前 GPU 利用率，CPU 利用率，显存占用，内存占用，以及 CPU 和 GPU 的温度。"

通过 AskUserQuestion 确认两个细节：
- 温度采集方案：GPU 温度 pynvml + CPU 温度 N/A（Windows psutil 不支持 sensors_temperatures，避免引入 wmi/LibreHardwareMonitor 等第三方库，符合"零第三方依赖起步"原则）
- 显示形式：折线历史 + 当前数值（每个 cell 顶部图例含色块+指标名+当前数值，下方 60s 折线历史）

## 三、上次执行（v0.0.15 硬件监控独立窗口 + menu_bar 回调模式）

### 核心改动

- **hardware_monitor.py 重写**：
  - 删除 v0.0.12 旧 HardwareMonitor widget 类（sidebar 底部常驻折线，已被 left panel 移除挂载）
  - HardwareMonitorWorker._collect() 扩展采集温度：新增 `cpu_temp`（Windows psutil 不支持 sensors_temperatures，永远 None）+ `gpu_temp`（pynvml `nvmlDeviceGetTemperature(handle, 0)` NVML_TEMPERATURE_GPU）
  - METRICS 从 4 元组扩展为 6 元组，新增 unit 字段（"%" / "°C"，温度与百分比数值范围 0-100 巧合一致可共用 y 轴）
  - 保留 collect_sample_sync() 测试辅助函数，扩展 cpu_temp/gpu_temp 字段
  - 只保留 Worker + 常量 + METRICS + collect_sample_sync，HardwareMonitorWindow + MonitorCell 独立到新文件

- **hardware_monitor_window.py 新建（独立 widget 文件，符合贴纸式原则）**：
  - `MonitorCell(QWidget)`：单指标 cell 自绘，固定高度 150px + 最小宽度 280px；顶部 20px 图例（色块 + 指标名 + 当前数值按 unit 切换 `f"{x:.0f}%"` / `f"{x:.0f}°C"` / `N/A`）+ 下方 60s 折线历史（25/50/75% 网格线 + None 段断开不连线 + 全 None 时画灰色 N/A 占位横线）
  - `HardwareMonitorWindow(QWidget)`：6 个 MonitorCell 2×3 网格 + 顶部标题"硬件监控 - 6 指标 60s 历史" + 固定 620×520 + 后台 worker；closeEvent 调 `worker.stop() + wait(2000)` 优雅退出；start()/stop() 方法管 worker 生命周期

- **menu_bar.py 扩展公开接口（callback 注入模式，类比 Toolbar status_callback）**：
  - `MenuBar.__init__` 新增 `monitor_callback: Callable[[], None] | None = None` 参数
  - 新增 `_build_monitor_menu()` 方法，"监控"菜单 + "打开监控"QAction（Ctrl+M 快捷键），`triggered.connect(monitor_callback)`
  - 不侵入既有 `_build_file_menu` / `_build_help_menu`，纯增量

- **main_window.py 改（贴纸式迁移）**：
  - 移除 left panel 中的 HardwareMonitor 挂载（`from q_agent.ui.hardware_monitor import HardwareMonitor` 删除 + `self.hardware_monitor = HardwareMonitor(left_panel)` + `left_layout.addWidget(self.hardware_monitor)` 删除 + `QTimer.singleShot(200, self.hardware_monitor.start)` 删除 + closeEvent 中的 `self.hardware_monitor.stop()` 删除）
  - left panel 现在只剩 sidebar，恢复 v0.0.9 前的极简结构
  - 新增 `from q_agent.ui.hardware_monitor_window import HardwareMonitorWindow`
  - 新增 `_hw_window: HardwareMonitorWindow | None = None` 字段（启动时为 None，按需实例化）
  - MenuBar 注入 `monitor_callback=self._open_hardware_monitor`
  - 新增 `_open_hardware_monitor()` 方法：若 `_hw_window is None` 则实例化；调 `start() + show() + raise_()`；已打开则再次 show + raise 激活，避免重复实例化
  - closeEvent 改为关闭 `_hw_window`（如有）避免 worker 线程悬挂

### 测试改造

- `tests/test_ui_hardware_monitor.py` 重写（删旧 HardwareMonitor widget 测试 + 加 MonitorCell/Window 14 例）：
  - HardwareMonitorWorker 构造 / collect_sample_sync 6 字段 / METRICS 6 元组
  - MonitorCell 构造 + set_sample 追加截断 + None 不崩 + paintEvent 三场景（空/部分/全）+ _draw_line None 断开 + 全 None 占位横线
  - HardwareMonitorWindow 构造（6 cell + worker + 固定尺寸）+ _on_sample 分发到 6 cell + start/stop 幂等
- `tests/test_ui_imports.py` 改 `test_main_window_constructs`：`w.hardware_monitor is not None` → `not hasattr(w, "hardware_monitor")` + `w._hw_window is None` + `w.menu._monitor_callback is not None`

### 工具链

- ruff format 修了 5 个文件格式（main_window.py + menu_bar.py + hardware_monitor.py + hardware_monitor_window.py + test_ui_hardware_monitor.py）
- ruff check 修了 2 个 E501（hardware_monitor.py + menu_bar.py docstring 长行）+ 1 个 UP035（Callable 从 typing 改为 collections.abc）
- mypy strict 全绿（43 源文件）
- pytest：148 通过，覆盖率 84.39%

### 版本升级 + 打包

- __version__ 0.0.14 → 0.0.15
- menu_bar _show_about 文案更新为 v0.0.15 + "活 UI 空壳 + 硬件监控独立窗口"
- PyInstaller 严格按 README.md 标准模板：`--onefile --windowed --clean --noconfirm --add-data "q_agent/assets;q_agent/assets" --hidden-import pynvml --collect-all pynvml --collect-all psutil q_agent/cli.py`
- 47MB 零外部依赖
- 删除 v0.0.14 子目录，仅留 v0.0.15
- 启动验证：`Q-agent.exe version` → `Q-agent 0.0.15`

### 记忆同步

- ADR-029 追加到 `memory/ADR架构决策记录.md`（硬件监控独立窗口 + menu_bar 回调模式，取代 ADR-026 中"sidebar 底部常驻挂载"部分，ADR-026 其余指标/数据源/采样/容错决策保留）
- 思维导图数据.jsonl 追加 v0.0.15 一行 → update_mindmap.py 重新渲染（23 存活 + 0 删除）
- 技能工具注册清单追加 v0.0.15 条目（HardwareMonitorWindow + MonitorCell + Worker 扩展 + MenuBar callback 注入 + MainWindow _open_hardware_monitor + ADR-029）
- 项目指导文件更新当前节点（v0.0.15 完成）+ 追加已完成节点
- 安装包/README.md 追加 v0.0.15 版本历史行
- 临时记忆备份编年史 + 重写为 v0.0.15 完成版

### 待办

- 方案归档到 `memory/方案/已执行/`
- git commit v0.0.15 代码 + 规则 + 记忆同步
- git push
- **等用户双击 v0.0.15 .exe 验证**：菜单栏"监控"→"打开监控"→独立窗口弹出，6 指标 2×3 网格折线图（CPU/GPU/VRAM/RAM 4 百分比 + CPU°C 灰色 N/A 占位横线 + GPU°C 红色折线），数值随时间更新，关闭窗口 worker 优雅退出

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度 + **贴纸式开发原则 v0.0.14**）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本 + ADR-016 方案 D + 设计系统规范 + UI 图标 + q_agent/ui/ 模块
- ✅ PyInstaller 打 v0.0.1~v0.0.15 .exe（按新规则只留 v0.0.15）
- ✅ UI 改进两轮 + 输入框动态高度
- ✅ 本地 Ollama 模型检测 + 下拉框本地/云端分组 + AI 气泡模型名标签
- ✅ llm 层对称骨架（OllamaClient 子类 + cloud stubs）
- ✅ 安装包规则改为只保留最新
- ✅ Ollama Cloud 转发模型分类修复（三组结构 + ModelEntry）
- ✅ 通过 Ollama 真实唤醒模型 + 流式批量刷新（ChatWorker + 500字/500ms 混合阈值）
- ✅ 内存释放 + 切换模型清空上下文 + 模型名 hash 颜色
- ✅ 加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ✅ release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）
- ✅ 硬件监控曲线 v0.0.12（sidebar 底部常驻）→ v0.0.14（left panel 一行挂载）→ **v0.0.15 独立窗口（menu_bar triggered 弹出）**
- ✅ menu_bar 回调注入模式（monitor_callback，类比 Toolbar status_callback）
- ✅ ADR-026 追加（v0.0.12 硬件监控曲线决策，"sidebar 底部常驻挂载"部分被 ADR-029 取代）
- ✅ ADR-027 追加（贴纸式开发原则）
- ✅ ADR-028 追加（v0.0.12 sidebar 容器化回退决策）
- ✅ ADR-029 追加（硬件监控独立窗口 + menu_bar 回调模式）
- ✅ CLAUDE.md 第二十一节贴纸式开发原则 + 第十七节打包命令固化

## 五、下一步

- [ ] 方案归档到 `memory/方案/已执行/`
- [ ] git commit v0.0.15 代码 + 规则 + 记忆同步
- [ ] git push
- [ ] **用户双击 v0.0.15 .exe 验证**：菜单"监控"→"打开监控"→独立窗口 6 指标折线
- [ ] 用户提下一个需求（候选：取消按钮接 ChatWorker.stop() / 设置持久化 QSettings / 多轮上下文裁剪 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接 OpenAI/Anthropic/Google API / 历史消息窗口策略 / 释放成功后下拉框不选中此模型 / 新建对话按钮实际行为 / 清空按钮实际行为 / 加载时长统计 + 慢响应警告 / 硬件监控采样间隔可配置 / 硬件监控历史窗口可配置 / 硬件监控报警阈值 / 硬件监控窗口置顶选项 / 硬件监控窗口可拖拽调整大小）——**所有新功能必须遵循贴纸式开发原则（CLAUDE.md 第二十一节）：独立 widget + 父容器一行挂载 + 既有模块零改动**

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（"历史版本长期保留"部分已被 ADR-020 取代）
- ADR-011：UI 矢量图资源隔离
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D
- ADR-017：UI 输入框动态高度策略
- ADR-018：本地 LLM 检测首版选 Ollama + urllib + QThread 异步
- ADR-019：模型下拉框本地/云端分组 + AI 气泡模型名标签
- ADR-020：llm 层对称骨架 + 安装包只保留最新
- ADR-021：Ollama Cloud 转发模型分类（remote_model / remote_host 字段判定）
- ADR-022：通过 Ollama 唤醒模型 + 流式批量刷新（500字/500ms 混合阈值）
- ADR-023：模型内存释放 + 切换清空上下文 + 模型名颜色
- ADR-024：加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ADR-025：release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）
- ADR-026：硬件监控曲线（"sidebar 底部常驻挂载"部分被 ADR-029 取代，其余指标/数据源/采样/渲染/容错决策保留）
- ADR-027：贴纸式开发原则（基座永不动 + 新功能独立 widget + 父容器一行挂载 + 架构级改动判定）
- ADR-028：v0.0.12 sidebar 容器化违反贴纸式原则的回退决策（取代 ADR-026 中"Sidebar 容器化"部分）
- ADR-029：硬件监控独立窗口 + menu_bar 回调模式（取代 ADR-026 中"sidebar 底部常驻挂载"部分）

## 七、未解决问题

- github.com push 网络问题（v0.0.14 push 成功；v0.0.15 待 push）
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed GUI 应用 console handle 继承）——不影响用户体验
- 取消按钮未接 ChatWorker.stop()（接口已留，v0.0.16+ 接）
- 历史消息首版全传（token 消耗大但简单），上下文裁剪留 v0.0.16+
- host 仍 hardcoded "http://localhost:11434"，QSettings 持久化留 v0.0.16+
- 设计规范 spacing/shadow 表是推测值
- CLOUD_PRESET 仍是硬编码占位，未接 OpenAI/Anthropic/Google API（cloud stubs 仅骨架）
- 模型选择未持久化（QSettings 落盘是下一候选需求）
- Ollama Cloud 判定依赖 remote_model/remote_host 字段，老版 Ollama 可能不返回，退化为 v0.0.6 行为（graceful，非问题）
- release_model 调用时如果模型正在 chat 生成中，Ollama 在当前生成完成后立即卸载（keep_alive=0 不强制中断当前生成）；验证步骤会检测到模型仍在 → 抛错提示"可能正在生成中，请稍后重试"
- 切换模型清空对话是硬规则，不保留历史；用户切回原模型需重新输入
- 模型名 hash 颜色可能碰撞（crc32 mod 8 同值），8 色调色板日常足够，碰撞概率 1/8 可接受
- 新建对话/清空按钮仍是活 UI 空壳，未接 chat_page._clear_messages
- LoadingDots 固定 dot_size=8 不随 DPI 缩放（高 DPI 屏可能偏小）
- LoadingDots 流动彩虹色相独立于模型色，与"模型名 hash 色"色彩系统并行（设计取舍，非冲突）
- **Ollama 进程级 CUDA context 不立即归还 OS**：release_model /api/ps 验证通过 + nvidia-smi VRAM 已归还，但任务管理器看 ollama.exe 进程的"专用 GPU 内存"列可能仍显示几 GB 占用，这是 Ollama 已知行为非项目 bug，状态栏文案已明确告知用户
- **硬件监控无 NVIDIA 显卡时 GPU/VRAM/GPU°C 永远 N/A**：pynvml 容错降级，AMD/Intel GPU 不支持（需引入额外依赖，等用户提需求）
- **硬件监控 CPU°C 在 Windows 永远 N/A**：psutil 不支持 sensors_temperatures（Linux/macOS 支持），避免引入 wmi/LibreHardwareMonitor 等第三方库，符合"零第三方依赖起步"原则
- **硬件监控采样间隔固定 1s 不可配置**：未来可在设置中调 1s/2s/5s
- **硬件监控历史窗口固定 60s 不可配置**：未来可调 60s/120s/300s
- **硬件监控不写盘持久化**：关闭重启后历史清空
- **硬件监控窗口固定 620×520 不可调整大小**：未来可加可拖拽 resize
- **硬件监控窗口不置顶**：未来可加 always-on-top 选项
- **v0.0.12/v0.0.13 的教训已通过 ADR-027/028 + CLAUDE.md 第二十一节明文化**：贴纸式开发原则 + 打包命令固化，下次 AI 在规则层面被拦住，不靠判断力