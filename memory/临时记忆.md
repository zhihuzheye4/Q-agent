<!--
权限：L3 完全权限
说明：AI 启动时必读本文件 + CLAUDE.md + memory/项目指导文件.md，其余 L1/L2 文件 AI 不读。
       会话末整体覆写；覆写前旧版本完整快照写入 编年史记忆.md（L1）。
       作用：让下次窗口快速回忆当前项目状态。
       风险：可被 AI Edit 误删 → 靠编年史兜底。
-->

# Q-agent 临时记忆

更新时间：2026-06-20 14:10

## 一、项目详情

- 项目名：Q-agent
- 路径：G:\agent
- 仓库：https://github.com/zhihuzheye4/Q-agent
- 语言：Python 3.10+
- 定位：类似 Claude Code 的桌面端 AI 工具，纯软件形式对接本地 LLM
- 五层结构：对话/交互 → 编排/规划 → 记忆 → 技能/工具 → 工具调用层
- 关键约束：永不沙箱（仅基本输入校验）、本地 LLM 优先、零第三方依赖起步、每里程碑打 .exe、分发零外部依赖硬规则、安装包只保留最新一代
- 当前阶段：v0.0.11 完成（release_model 卸载验证：/api/ps 轮询确认 + 状态栏文案明确），等待用户提下一个需求

## 二、上次讨论

用户报告 v0.0.9 内存释放功能失败：
> 内存释放功能失败
> 点了释放按钮，选择yes，但是任务管理器中显示，GPU占用依然存在，没有被释放。
> 模型类型：本地模型

排查结论：
- 代码本身工作正常：curl 直接调 /api/generate keep_alive=0 → /api/ps 立即变空，Python release_model() 同样工作
- nvidia-smi 显示 VRAM 从 5.6GB 降到 1GB（剩余 1GB 是 explorer/NVIDIA Overlay 等系统进程）
- 真正原因：**Ollama 进程级 CUDA context 不立即归还 OS**——/api/ps 显示模型已卸载，nvidia-smi 显示 VRAM 已归还，但任务管理器看 ollama.exe 进程的"专用 GPU 内存"列可能仍显示几 GB 占用（CUDA pool 不立即归还，需等一段时间或进程退出）
- v0.0.9 状态栏文案"已释放 XXX 内存"过于笼统，没有给用户可信的"真的卸载了"反馈

用户回复"开始"确认实施改进方案：release_model 加 /api/ps 验证步骤 + 状态栏文案明确说明。

## 三、上次执行

- `q_agent/llm/ollama.py`：
  - import `time` 用于 sleep
  - 新增 `_is_model_loaded(model, host="http://localhost:11434", timeout=5.0) -> bool`：GET /api/ps 解析 models 列表，判断指定模型是否仍在内存
  - `release_model` 加 4 个新参数：`verify: bool = True` / `verify_attempts: int = 3` / `verify_interval: float = 0.3`
  - 卸载请求成功后，若 verify=True 则轮询 /api/ps（间隔 300ms × 3 次），全部尝试后仍在 → 抛 `OllamaError("Ollama 接受卸载请求但 XXX 仍在内存（可能正在生成中，请稍后重试）")`
- `q_agent/ui/toolbar.py`：
  - `_on_released` 状态栏文案改为 `"已卸载 XXX 出 Ollama（API 验证通过，VRAM 已归还；任务管理器进程级 GPU 内存可能延迟显示）"`
  - `_on_release_failed` 文档说明区分"连接失败"与"卸载未生效"两种语义
- 测试 `tests/test_llm_ollama.py` 5 例新增/改写：
  - test_release_model_happy 改用 verify=False 跳过验证专门测 generate payload
  - test_release_model_verifies_unload_success（generate OK + /api/ps 空 → 通过）
  - test_release_model_verifies_unload_still_loaded（3 次轮询后仍含模型 → OllamaError）
  - test_release_model_verify_retries_until_success（前 2 次含 + 第 3 次空 → 通过，验证轮询）
  - test_release_model_verify_disabled_skips_ps（verify=False 只调 generate）
  - fake_urlopen 兼容 req 为 Request 对象（generate 调用）或字符串（/api/ps 调用）两种情况：`url = req if isinstance(req, str) else req.full_url`
- 工具链全绿：ruff format + ruff check + mypy strict + pytest -q（134 测试，覆盖率 85.43%）
- __version__ 0.0.10 → 0.0.11
- v0.0.11 .exe 打包：47MB 零外部依赖，PyInstaller --onefile --windowed --clean
- 删除 v0.0.10 子目录，仅留 v0.0.11（按"只留最新"规则）
- 安装包/README.md 版本历史表追加 v0.0.11 行
- ADR-025 追加（release_model 卸载验证决策 + 排查记录 + Ollama 进程级 CUDA context 不归还 OS 说明）
- 待办：思维导图数据追加 / 技能注册清单追加 / 项目指导文件更新 / 方案归档 / commit + push

## 四、当前进度

- ✅ 记忆系统骨架（10 文件 + 权限三原则 + 思维导图脚本化）
- ✅ 项目框架搭建（工具链 + 核心模块骨架 + 项目指导文件 + 方案文件夹）
- ✅ 独立审查修复（safety 重写 + .gitignore 补全 + 测试盲区补全）
- ✅ 新规则制定（安装包 + UI 矢量图 + 指令执行自由度）
- ✅ 分发零外部依赖硬规则（CLAUDE.md 第二十节 + ADR-015）
- ✅ 矢量图生成脚本 + ADR-016 方案 D + 设计系统规范 + UI 图标 + q_agent/ui/ 模块
- ✅ PyInstaller 打 v0.0.1~v0.0.11 .exe（按新规则只留 v0.0.11）
- ✅ UI 改进两轮 + 输入框动态高度
- ✅ 本地 Ollama 模型检测 + 下拉框本地/云端分组 + AI 气泡模型名标签
- ✅ llm 层对称骨架（OllamaClient 子类 + cloud stubs）
- ✅ 安装包规则改为只保留最新
- ✅ Ollama Cloud 转发模型分类修复（三组结构 + ModelEntry）
- ✅ 通过 Ollama 真实唤醒模型 + 流式批量刷新（ChatWorker + 500字/500ms 混合阈值）
- ✅ 内存释放 + 切换模型清空上下文 + 模型名 hash 颜色
- ✅ 加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ✅ release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）
- ✅ ADR-025 追加

## 五、下一步

- [ ] git commit v0.0.11 代码 + 记忆同步
- [ ] git push（GitHub 网络问题仍未解决，需用户介入）
- [ ] 用户提下一个需求（候选：取消按钮接 ChatWorker.stop() / 设置持久化 QSettings / 多轮上下文裁剪 / 字体优化 / 主题切换 / 扩展 LM Studio 后端 / 接 OpenAI/Anthropic/Google API / 历史消息窗口策略 / 释放成功后下拉框不选中此模型 / 新建对话按钮实际行为 / 清空按钮实际行为 / 加载时长统计 + 慢响应警告）

## 六、关键决策（详见 ADR）

- ADR-001：记忆权限三原则 L1/L2/L3
- ADR-002：编年史作为临时记忆安全网
- ADR-003：思维导图脚本化
- ADR-004：项目记忆体系为 Q-agent 专属
- ADR-005：项目框架搭建策略
- ADR-006：永不沙箱
- ADR-007：增量开发哲学
- ADR-008：方案文件夹机制
- ADR-009：项目指导文件机制
- ADR-010：可执行安装包机制（"历史版本长期保留"部分已被 ADR-020 取代）
- ADR-011：UI 矢量图资源隔离
- ADR-012：指令执行自由度规则
- ADR-013：UI 技术栈选 PySide6
- ADR-014：矢量图脚本预制策略（已被 ADR-016 部分取代）
- ADR-015：分发零外部依赖硬规则
- ADR-016：图标调用方案 D
- ADR-017：UI 输入框动态高度策略
- ADR-018：本地 LLM 检测首版选 Ollama + urllib + QThread 异步
- ADR-019：模型下拉框本地/云端分组 + AI 气泡模型名标签
- ADR-020：llm 层对称骨架 + 安装包只保留最新
- ADR-021：Ollama Cloud 转发模型分类（remote_model / remote_host 字段判定）
- ADR-022：通过 Ollama 唤醒模型 + 流式批量刷新（500字/500ms 混合阈值）
- ADR-023：模型内存释放 + 切换清空上下文 + 模型名颜色
- ADR-024：加载指示器（三点跳动 + 流动彩虹 + 半透明）
- ADR-025：release_model 卸载验证（/api/ps 轮询确认 + 状态栏文案明确）

## 七、未解决问题

- github.com push 网络问题仍未解决，本地领先 origin/main 多个 commit 待推（错误 056 第三次复发 + 错误 057 RPC connection reset）
- .exe stdout 在外部进程调用时不输出（PyInstaller --onefile + --windowed GUI 应用 console handle 继承）——不影响用户体验
- 取消按钮未接 ChatWorker.stop()（接口已留，v0.0.12+ 接）
- 历史消息首版全传（token 消耗大但简单），上下文裁剪留 v0.0.12+
- host 仍 hardcoded "http://localhost:11434"，QSettings 持久化留 v0.0.12+
- 设计规范 spacing/shadow 表是推测值
- CLOUD_PRESET 仍是硬编码占位，未接 OpenAI/Anthropic/Google API（cloud stubs 仅骨架）
- 模型选择未持久化（QSettings 落盘是下一候选需求）
- Ollama Cloud 判定依赖 remote_model/remote_host 字段，老版 Ollama 可能不返回，退化为 v0.0.6 行为（graceful，非问题）
- release_model 调用时如果模型正在 chat 生成中，Ollama 在当前生成完成后立即卸载（keep_alive=0 不强制中断当前生成）；v0.0.11 验证步骤会检测到模型仍在 → 抛错提示"可能正在生成中，请稍后重试"
- 切换模型清空对话是硬规则，不保留历史；用户切回原模型需重新输入
- 模型名 hash 颜色可能碰撞（crc32 mod 8 同值），8 色调色板日常足够，碰撞概率 1/8 可接受
- 新建对话/清空按钮仍是活 UI 空壳，未接 chat_page._clear_messages
- LoadingDots 固定 dot_size=8 不随 DPI 缩放（高 DPI 屏可能偏小）
- LoadingDots 流动彩虹色相独立于模型色，与"模型名 hash 色"色彩系统并行（设计取舍，非冲突）
- **Ollama 进程级 CUDA context 不立即归还 OS**：release_model /api/ps 验证通过 + nvidia-smi VRAM 已归还，但任务管理器看 ollama.exe 进程的"专用 GPU 内存"列可能仍显示几 GB 占用，这是 Ollama 已知行为非项目 bug，状态栏文案已明确告知用户