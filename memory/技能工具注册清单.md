<!--
权限：L3 完全权限
说明：Q-agent 框架已实现的技能/工具/Agent 注册清单。
       AI 可读可写可改可删（编排层需查询以决定调度）。
       风险：靠 git 提交历史兜底，定期 commit。
       格式：每个技能/工具一节，含名称/签名/版本/调用统计。
-->

# Q-agent 技能/工具/Agent 注册清单

更新时间：2026-06-19 19:55

## 技能层（Skills）— 面向用户的命令

### echo

- **名称**：echo
- **描述**：回显输入文本
- **签名**：`echo(text: str) -> str`
- **模块**：`q_agent.skills.builtin.echo`
- **注册方式**：`@skill(name="echo", desc="回显输入文本")`
- **状态**：已实现，用于冒烟测试
- **调用统计**：暂无

## 工具层（Tools）— LLM 调用的底层操作

（暂无已注册工具，框架阶段仅就位 stub 接口）

## Agent 层（Sub-Agents）

（暂无，等用户提"多 Agent 协作"需求）

## 开发辅助脚本（dev-only，不入分发包）

> 这些脚本在开发期辅助生成代码/资源，本身不在 `q_agent` 包内，不会进 `.exe`。

### generate_icons

- **路径**：`scripts/generate_icons.py`
- **用途**：UI 矢量图生成（SVG sprite + 可选 PNG + manifest.json）
- **调用方式**：
  - `python scripts/generate_icons.py`（默认生成 SVG）
  - `python scripts/generate_icons.py --png`（按需，转 PNG 用 PySide6.QtSvg）
  - `python scripts/generate_icons.py --clean`（清理测试产物）
- **技术栈**：PySide6 + QtSvg（延迟 import，仅 `--png` 时需）
- **设计哲学**：预制调用而非实时渲染；PNG 转换按需使用；测试产物用完即删
- **状态**：已实现，5 级递进复杂度测试通过（L1-L5 全部稳定输出，L5 接近上限但可控）
- **当前产物**：无（测试 SVG 已用 `--clean` 清理，等 UI 真正需要图标时再生成正式图标）